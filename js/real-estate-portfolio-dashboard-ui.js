// Mobile-first real-estate portfolio headquarters dashboard.
(function(){'use strict';const modules=globalThis.__capitalismTycoonModules;if(!modules?.realEstatePortfolioDashboard||!modules?.playerEngineBridge?.getEngine)throw new Error('portfolio dashboard and player bridge are required.');if(!modules?.uiEnhancerRegistry?.registerUIEnhancer)throw new Error('ui-enhancer-registry.js must load before real-estate-portfolio-dashboard-ui.js.');if(modules.realEstatePortfolioDashboardUI)throw new Error('portfolio dashboard UI already registered.');const bridge=modules.playerEngineBridge,esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),money=v=>`${Math.round(Number(v)||0).toLocaleString('ja-JP')}円`,pct=v=>`${(Number(v||0)*100).toFixed(1)}%`;function render(){const e=bridge.getEngine(),screen=document.querySelector('[data-screen="assets"]');if(!e||!screen)return;screen.querySelector('[data-real-estate-portfolio-dashboard]')?.remove();const settings=e.g?.realEstatePortfolioSettings||{owner:'all',sort:'risk'},r=e.getRealEstatePortfolio?.(settings);if(!r||!r.totals.count)return;const box=document.createElement('section');box.dataset.realEstatePortfolioDashboard='';box.className='card';box.innerHTML=`<h2>不動産ポートフォリオ本部</h2><p class="muted">${r.totals.count}物件 · 資産${money(r.totals.value)} · 負債${money(r.totals.debt)} · 純資産${money(r.totals.equity)}</p><p class="muted">稼働率${pct(r.totals.occupancy)} · LTV ${pct(r.totals.ltv)} · DSCR ${r.totals.dscr===999?'無借入':Number(r.totals.dscr).toFixed(2)} · 年間NOI ${money(r.totals.noi)}</p><div class="button-row">${[['all','全物件'],['company','会社'],['personal','個人']].map(([id,label])=>`<button class="btn secondary small" style="min-height:44px" data-portfolio-owner="${id}">${label}</button>`).join('')}${[['risk','危険度'],['value','資産価値'],['equity','純資産'],['occupancy','空室優先']].map(([id,label])=>`<button class="btn secondary small" style="min-height:44px" data-portfolio-sort="${id}">${label}</button>`).join('')}</div><div>${r.rows.map(x=>`<article class="item"><strong>${esc(x.name)}</strong><p class="muted">${x.owner==='personal'?'個人':'会社'} · 価値${money(x.value)} · 純資産${money(x.equity)} · LTV ${pct(x.ltv)} · DSCR ${x.dscr===999?'無借入':Number(x.dscr).toFixed(2)}</p><p class="muted">稼働${x.occupancy?'入居中':'空室'} · 状態${Math.round(x.condition)} · 危険度${x.risk}${x.alerts.length?` · ${x.alerts.map(esc).join(' / ')}`:''}</p></article>`).join('')}</div>`;screen.prepend(box);}function queue(){return modules.uiEnhancerRegistry.runUIEnhancers();}document.addEventListener('click',ev=>{const owner=ev.target.closest?.('[data-portfolio-owner]'),sort=ev.target.closest?.('[data-portfolio-sort]');if(!owner&&!sort)return;ev.preventDefault();const e=bridge.getEngine(),s=e?.g?.realEstatePortfolioSettings||{owner:'all',sort:'risk'};e?.setRealEstatePortfolioView?.(owner?.dataset.portfolioOwner||s.owner,sort?.dataset.portfolioSort||s.sort);e?.save?.();e?.emit?.();queue();});modules.realEstatePortfolioDashboardUI=Object.freeze({render,queue});modules.uiEnhancerRegistry.registerUIEnhancer({id:'real-estate-portfolio-dashboard-ui',enhance:()=>render()});})();
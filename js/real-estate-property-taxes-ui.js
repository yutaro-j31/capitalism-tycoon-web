// Mobile-first property tax controls.
(function(){'use strict';const modules=globalThis.__capitalismTycoonModules;if(!modules?.realEstatePropertyTaxes||!modules?.playerEngineBridge?.getEngine)throw new Error('property taxes and player bridge are required.');if(modules.realEstatePropertyTaxesUI)throw new Error('property tax UI already registered.');const bridge=modules.playerEngineBridge,esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),money=v=>`${Math.round(Number(v)||0).toLocaleString('ja-JP')}円`;let queued=false;function render(){queued=false;const e=bridge.getEngine();if(!e)return;for(const article of document.querySelectorAll('[data-real-estate-property]')){const id=article.dataset.realEstateProperty;article.querySelector('[data-property-tax-controls]')?.remove();const p=(e.g.properties||[]).find(x=>x.id===id),regimes=e.getPropertyTaxRegimes?.(id)||[];if(!p)continue;const box=document.createElement('div');box.dataset.propertyTaxControls='';box.className='item';box.innerHTML=`<p class="muted">固定資産税: ${esc(p.propertyTaxRegimeID||'未設定')} · 未払${money(p.propertyTaxAccrued)} · 累計納付${money(p.propertyTaxPaidTotal)}</p><div class="button-row">${regimes.map(r=>`<button class="btn secondary small" style="min-height:44px" data-property-tax-regime="${esc(r.id)}" data-property-id="${esc(id)}">${esc(r.label)} ${money(r.weeklyAccrual)}/週</button>`).join('')}${Number(p.propertyTaxAccrued||0)>0?`<button class="btn primary small" style="min-height:44px" data-property-tax-pay data-property-id="${esc(id)}">税金を納付</button>`:''}</div>`;article.appendChild(box);}}function queue(){if(queued)return;queued=true;(typeof queueMicrotask==='function'?queueMicrotask:fn=>setTimeout(fn,0))(render);}document.addEventListener('click',ev=>{const regime=ev.target.closest?.('[data-property-tax-regime]'),pay=ev.target.closest?.('[data-property-tax-pay]');if(!regime&&!pay)return;ev.preventDefault();const e=bridge.getEngine();if(regime)e?.setPropertyTaxRegime?.(regime.dataset.propertyId,regime.dataset.propertyTaxRegime);else e?.payPropertyTax?.(pay.dataset.propertyId);e?.save?.();e?.emit?.();queue();});if(typeof MutationObserver==='function'){const app=document.getElementById('app');if(app)new MutationObserver(queue).observe(app,{childList:true,subtree:true});}queue();modules.realEstatePropertyTaxesUI=Object.freeze({render,queue});})();
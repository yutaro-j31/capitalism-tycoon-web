const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const s=modules.engine.createInitialState({configured:true});s.companyCash=100000000;s.stores.push({id:'s1',businessID:'ramen',prefID:'tokyo',name:'R',status:'open',condition:100,operatingHours:3});modules.workforce.migrateV7(s);modules.workforce.recompute(s);let base=modules.market.calculateMarkets(s).byStore.s1;const t=s.workforceTeams.find(x=>x.storeID==='s1');t.headcount=1;t.managerHeadcount=0;t.fatigue=85;modules.workforce.recompute(s);let low=modules.market.calculateMarkets(s).byStore.s1;assert(low.effectiveCapacity<base.effectiveCapacity);assert(low.staffLostDemand>0);assert(low.serviceQuality<base.serviceQuality);console.log('workforce market integration ok');

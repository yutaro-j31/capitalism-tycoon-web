const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});function setup(store=false){const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.hasHeadOffice=true;e.g.officeCapacity=100;e.g.departments.hr={...modules.data.MASTER.departments.find(d=>d.id==='hr'),established:true};e.g.departmentStaff.hr=3;if(store)e.g.stores.push({id:'s0',businessID:'ramen',prefID:'tokyo',name:'s0',status:'open',condition:100,operatingHours:3,openingWeek:1,weeksToOpen:0});modules.workforce.migrateV7(e.g);return e;}let e=setup();let t=e.g.workforceTeams.find(x=>x.departmentID==='hr');const oldSkill=t.averageSkill,oldAvailable=t.availableHeadcount;e.g.workforceCandidates.push({candidateID:'hi',roleID:'hr',departmentID:'hr',headcount:2,averageSkill:95,averageExperience:90,weeklySalaryPerPerson:90000,recruitmentFee:100,onboardingWeeks:1,expectedMorale:80,remoteCompatibility:.8,expiresWeek:5,status:'open'});e.hireWorkforceCandidate('hi');t=e.g.workforceTeams.find(x=>x.departmentID==='hr');assert.equal(t.averageSkill,oldSkill);assert.equal(t.availableHeadcount,oldAvailable);e.advanceWeek(false);t=e.g.workforceTeams.find(x=>x.departmentID==='hr');assert(t.averageSkill>oldSkill);const after=t.averageSkill;e.advanceWeek(false);assert.equal(t.averageSkill,after);e=setup(true);let st=e.g.workforceTeams.find(x=>x.storeID==='s0');const sOld=st.averageSkill;e.g.workforceCandidates.push({candidateID:'his',roleID:'store-ops',storeID:'s0',targetStoreID:'s0',headcount:1,averageSkill:95,averageExperience:90,weeklySalaryPerPerson:22000,recruitmentFee:100,onboardingWeeks:1,expectedMorale:80,remoteCompatibility:0,expiresWeek:5,status:'open'});e.hireWorkforceCandidate('his');st=e.g.workforceTeams.find(x=>x.storeID==='s0');assert.equal(st.averageSkill,sOld);e.advanceWeek(false);assert(e.g.workforceTeams.find(x=>x.storeID==='s0').averageSkill>sOld);console.log('workforce onboarding activation ok');

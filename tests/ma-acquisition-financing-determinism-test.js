'use strict';const assert=require('node:assert/strict');let calls=0;const{createScenario}=require('./ma-acquisition-financing-fixture'),s=createScenario({loadOptions:{random:()=>{calls++;return .5;}}}),{game,deal,modules}=s,m=modules.maAcquisitionFinancing,h={engine:game,companyValue:game.companyValue(),borrowRate:game.companyBorrowRate(),creditLimit:game.companyCreditLimit()};calls=0;const a=m.buildCapacity(game.g,deal,h),b=m.buildCapacity(game.g,deal,h);assert.deepEqual(a,b);const p=m.buildPlan(game.g,deal,{presetID:'balanced'},h),q=m.buildPlan(game.g,deal,{presetID:'balanced'},h);assert.deepEqual(p,q);assert.deepEqual(m.previewPostDeal(game.g,deal,p,h),m.previewPostDeal(game.g,deal,q,h));assert.equal(calls,0);const r=JSON.parse(JSON.stringify(game.g));r.finance.loans.reverse();r.finance.transactions.reverse();r.maSubsidiaries.reverse();assert.deepEqual(m.buildCapacity(r,r.maDealRooms[0],{companyValue:h.companyValue,borrowRate:h.borrowRate,creditLimit:h.creditLimit}),a);console.log('ma acquisition financing determinism tests passed');

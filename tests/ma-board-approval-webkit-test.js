const fs=require('node:fs');const path=require('node:path');const out=path.resolve(__dirname,'..','artifacts','ma-board-approval-webkit','README.txt');fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,'Run this Playwright WebKit smoke in GitHub Actions or an environment with @playwright/test installed. This file does not synthesize success artifacts.\n');try{require.resolve('@playwright/test');}catch(e){console.error('Playwright is not installed; WebKit smoke not executed locally.');process.exit(1);}console.log('Playwright available; scenario should be exercised by CI browser environment.');

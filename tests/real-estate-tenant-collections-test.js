const assert=require('assert'),fs=require('fs'),vm=require('vm');
const files=['js/real-estate-tenant-contracts.js','js/real-estate-tenant-leasing.js','js/real-estate-tenant-operations.js','js/real-estate-tenant-renewals.js','js/real-estate-tenant-collections.js'];
class E{constructor(g){this.g=g;}runTransaction(fn){return fn();}normalize(){return this.g;}updateParityWeekly(){} }
const context={globalThis:{__capitalismTycoonModules:{engine:{TycoonEngine:E},finance:{event(){}},realEstateSafetyCertification:{ensure(g){if(!g.realEstateDevelopment)g.realEstateDevelopment={};return g.realEstateDevelopment;}}}},console};context.globalThis.globalThis=context.globalThis;vm.createContext(context);for(const f of files)vm.runInContext(fs.readFileSync(f,'utf8'),context);
const g={week:10,companyCash:1e7,personalCash:1e7,properties:[{id:'p1',owner:'company',realEstate:{rentalEnabled:true,condition:.8},value:1e8}],realEstateDevelopment:{tenantContracts:[{contractID:'c1',propertyID:'p1',owner:'company',profileID:'stable',status:'active',monthlyRent:500000,depositBalance:0,arrears:800000,missedWeeks:2,renewalChance:.7,satisfaction:70,openComplaints:0}]}};
const e=new E(g);e.normalize();assert.equal(e.getPropertyTenantCollectionOptions('p1').length,3);const before=g.companyCash,result=e.collectPropertyTenantArrears('p1','reminder');assert(result);assert.equal(g.companyCash,before-5000+result.recovered);assert.equal(e.collectPropertyTenantArrears('p1','reminder'),false);const saved=JSON.parse(JSON.stringify(g)),e2=new E(saved);e2.normalize();assert.deepStrictEqual(JSON.parse(JSON.stringify(e2.g)),saved);for(let i=0;i<520;i++){e2.g.week++;e2.g.realEstateDevelopment.tenantContracts[0].arrears=10000;e2.g.realEstateDevelopment.tenantContracts[0].lastCollectionWeek=0;e2.collectPropertyTenantArrears('p1','reminder');}assert(e2.g.realEstateDevelopment.tenantCollectionHistory.length<=500);assert(Number.isFinite(e2.g.companyCash));const loader=fs.readFileSync('js/real-estate-tenant-renewals.js','utf8'),ui=fs.readFileSync('js/real-estate-tenant-collections-ui.js','utf8');assert(!/document\.createElement\(\s*['"]script['"]\s*\)/.test(loader));assert(/min-height:44px/.test(ui));assert(!/Math\.random|Date\.now/.test(fs.readFileSync('js/real-estate-tenant-collections.js','utf8')));console.log('tenant collections regression passed');

const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.finance=modules.finance.defaultFinanceState(e.g);e.g.hasHeadOffice=true;e.g.departments.operations={...modules.data.MASTER.departments.find(d=>d.id==='operations'),established:true};e.g.departmentStaff.operations=20;modules.workforce.migrateV7(e.g);const t=e.g.workforceTeams.find(x=>x.departmentID==='operations');t.fatigue=100;t.morale=0;t.managerHeadcount=0;t.requiredWorkload=999999;e.advanceWeek(false);assert(t.lastTurnoverHeadcount>=0);if(t.lastTurnoverHeadcount>0){assert(t.lastTurnoverCost>0);assert.equal(e.g.finance.transactions.filter(x=>x.sourceType==='weekly-severanceCost'&&x.week===e.g.week).length,1);}assert(t.managerHeadcount<=t.headcount);assert(t.onboardingHeadcount<=t.headcount);assert.equal(e.g.departmentStaff.operations,t.headcount);console.log('workforce turnover finance ok');

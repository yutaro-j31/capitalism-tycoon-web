const assert = require('node:assert/strict'); const { loadGame } = require('./harness'); const { modules }=loadGame(); const {createInitialState}=modules.engine; const market=modules.market;
const s=createInitialState({configured:true}); const t=s.tenants.find(t=>t.businessID==='ramen'&&t.prefID==='tokyo'); const base={businessID:'ramen',prefID:'tokyo',status:'open',tenantID:t.id,condition:100,operatingHours:3}; s.stores=[{...base,id:'s1',name:'A'}]; const one=market.calculateMarkets(s); s.stores.push({...base,id:'s2',name:'B'}); const two=market.calculateMarkets(s); assert(two.byStore.s1.marketShare < one.byStore.s1.marketShare); assert.equal(Object.values(two.byMarket)[0].marketPotential,Object.values(one.byMarket)[0].marketPotential); assert(two.businessSummary.ramen.marketShare<=1); s.stores=s.stores.filter(x=>x.id!=='s2'); const closed=market.calculateMarkets(s); assert(closed.byStore.s1.potentialDemand>=two.byStore.s1.potentialDemand);
const t2=s.tenants.find(t=>t.businessID==='ramen'&&t.prefID!=='tokyo'); s.stores.push({...base,id:'s3',prefID:t2.prefID,tenantID:t2.id,name:'C'}); const diff=market.calculateMarkets(s); assert(Object.keys(diff.byMarket).length>=2);
console.log('cannibalization ok');

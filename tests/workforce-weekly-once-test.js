const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.hasHeadOffice=true;e.g.departments.hr={...modules.data.MASTER.departments.find(d=>d.id==='hr'),established:true};e.g.departmentStaff.hr=2;modules.workforce.migrateV7(e.g);modules.data.MISSION_DEFS.push({id:'wf_fake_1',title:'fake1',reward:1,check:()=>true},{id:'wf_fake_2',title:'fake2',reward:1,check:()=>true});e.advanceWeek(false);assert.equal(e.g.workforceUpdateCountForWeek,1);const before=e.g.lastWorkforceUpdateResult;modules.workforce.updateEndOfWeek(e.g);assert.equal(e.g.workforceUpdateCountForWeek,1);assert.deepEqual(e.g.lastWorkforceUpdateResult,before);console.log('workforce weekly once ok');

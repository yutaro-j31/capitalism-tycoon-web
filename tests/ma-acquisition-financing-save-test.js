'use strict';const assert=require('node:assert/strict');const{createScenario}=require('./ma-acquisition-financing-fixture');const{game,deal,ctx,engineModule,modules}=createScenario();assert.equal(game.selectMAAcquisitionFinancing(deal.id,{presetID:'balanced'}),true);assert.equal(game.approveMAClosing(deal.id),true);const key=deal.acquisitionFinancingPlan.planKey;game.save();let loaded=engineModule.TycoonEngine.load();assert.equal(loaded.g.maDealRooms[0].acquisitionFinancingPlan.planKey,key);loaded.save(1);assert.ok(ctx.__localStorageData.get(`${engineModule.SAVE_KEY}_slot_1`));loaded.g.maDealRooms[0].acquisitionFinancingPlan=null;assert.equal(loaded.loadSlot(1),true);assert.equal(loaded.g.maDealRooms[0].acquisitionFinancingPlan.planKey,key);const raw=JSON.stringify(loaded.g),imported=new modules.engine.TycoonEngine();imported.importSave(raw);assert.equal(imported.g.maDealRooms[0].acquisitionFinancingPlan.planKey,key);const legacy=JSON.parse(raw);delete legacy.maDealRooms[0].acquisitionFinancingPlan;delete legacy.maDealRooms[0].boardApproval;legacy.saveVersion=9;const m=modules.engine.migrateSave(legacy);assert.equal(m.ok,true);assert.equal(m.state.maDealRooms[0].acquisitionFinancingPlan,undefined);assert.equal(engineModule.SAVE_KEY,'capitalism_tycoon_web_v1');assert.equal(m.state.saveVersion,9);console.log('ma acquisition financing save tests passed');

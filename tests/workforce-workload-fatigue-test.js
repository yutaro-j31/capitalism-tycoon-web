const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const s=modules.engine.createInitialState({configured:true});s.hasHeadOffice=true;s.departments.operations={...modules.data.MASTER.departments.find(d=>d.id==='operations'),established:true};s.departmentStaff.operations=1;for(let i=0;i<20;i++)s.stores.push({id:'r'+i,businessID:'ramen',prefID:'tokyo',name:'r'+i,status:'open',condition:100,operatingHours:3});modules.workforce.migrateV7(s);let t=s.workforceTeams.find(x=>x.departmentID==='operations');t.headcount=1;t.availableHeadcount=1;t.fatigue=10;modules.workforce.recompute(s);const deptReq=s.workforceResultsByDepartmentID.operations.requiredWorkload;assert(Math.abs(deptReq-t.requiredWorkload)<.001);const before=t.fatigue;modules.workforce.updateEndOfWeek(s);assert(t.fatigue>before);assert(t.overtimeHours>0);s.week++;s.lastWorkforceUpdateWeek=0;s.stores=[];t.requiredWorkload=0;t.fatigue=50;modules.workforce.updateEndOfWeek(s);assert(t.fatigue<50);console.log('workforce workload fatigue ok');

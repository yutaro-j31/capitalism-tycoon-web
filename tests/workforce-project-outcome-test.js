const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});function setup(){const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=1e9;e.g.hasHeadOffice=true;e.g.officeCapacity=200;for(const d of modules.data.MASTER.departments){e.g.departments[d.id]={...d,established:true};e.g.departmentStaff[d.id]=10;}e.g.stores.push({id:'s0',businessID:'ramen',prefID:'tokyo',name:'s0',status:'open',condition:100,operatingHours:3,openingWeek:1,weeksToOpen:0});modules.workforce.migrateV7(e.g);return e;}for(const tmpl of modules.workforce.PROJECT_TEMPLATES){const e=setup();modules.workforce.recompute(e.g);const beforeAdj=modules.workforce.storeAdjustment(e.g,e.g.stores[0],2000);const beforeWork=modules.workforce.workload(e.g,'procurement');e.startWorkforceProject(tmpl.templateID);const p=e.g.workforceProjects[0];while(p.status!=='completed')e.advanceWeek(false);const afterAdj=modules.workforce.storeAdjustment(e.g,e.g.stores[0],2000);const afterWork=modules.workforce.workload(e.g,'procurement');const improved=(afterAdj.staffLimitedCapacity>beforeAdj.staffLimitedCapacity)||(afterAdj.serviceQualityAdjustment>beforeAdj.serviceQualityAdjustment)||(afterWork<beforeWork);assert(improved,tmpl.templateID);}console.log('workforce project outcome ok');

const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame();const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.finance=modules.finance.defaultFinanceState(e.g);e.g.hasHeadOffice=true;e.g.departments.dx={...modules.data.MASTER.departments.find(d=>d.id==='dx'),established:true};e.g.departmentStaff.dx=8;modules.workforce.migrateV7(e.g);assert.equal(e.startWorkforceProject('dx-rollout'),true);const p=e.g.workforceProjects[0];e.advanceWeek(false);assert(p.spentBudget>0);assert.equal(e.g.finance.transactions.filter(t=>t.sourceType==='weekly-projectCost'&&t.week===e.g.week).length,1);assert.equal(modules.finance.validate(e.g).ok,true,modules.finance.validate(e.g).errors.join('\n'));const spent=p.spentBudget;e.setWorkforceProjectStatus(p.projectID,'paused');e.advanceWeek(false);assert.equal(p.spentBudget,spent);console.log('workforce project finance ok');

'use strict';const assert=require('node:assert/strict');const{createScenario}=require('./ma-acquisition-financing-fixture');let s=createScenario({reports:[{week:23,expenses:5000000,profit:6000000,sales:20000000}]}),{game,deal,modules}=s,c=game.maAcquisitionFinancingCapacity(deal.id);assert.equal(c.minimumCashBuffer,20000000);assert.equal(c.maximumCashContribution,game.g.companyCash-c.minimumCashBuffer-c.estimatedFees);assert.equal(typeof c.available,'boolean');for(const[k,v]of Object.entries(c))if(typeof v==='number')assert.ok(Number.isFinite(v),k);s=createScenario({publicCompany:false,stockPrice:0});({game,deal}=s);c=game.maAcquisitionFinancingCapacity(deal.id);assert.equal(c.maximumEquityRaise,0);assert.equal(game.maAcquisitionFinancingPresets(deal.id).find(x=>x.presetID==='balanced').requestedEquityAmount,0);s=createScenario({financeMutator:f=>{f.loans.push({loanID:'a',status:'active',scheduledPrincipalPayment:3000000,outstandingPrincipal:10000000},{loanID:'r',status:'repaid',scheduledPrincipalPayment:99000000,outstandingPrincipal:0});}});({game,deal,modules}=s);assert.equal(modules.maAcquisitionFinancing.minimumCashBuffer(game.g),12000000);game.g.finance.bankCovenants={...(game.g.finance.bankCovenants||{}),status:'breach'};c=modules.maAcquisitionFinancing.buildCapacity(game.g,deal,{companyValue:game.companyValue(),borrowRate:game.companyBorrowRate(),creditLimit:game.companyCreditLimit()});assert.equal(c.maximumAdditionalDebt,0);assert.ok(c.blockers.includes('covenant-breach'));console.log('ma acquisition financing capacity tests passed');

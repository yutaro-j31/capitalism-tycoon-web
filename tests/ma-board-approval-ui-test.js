const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {ctx,modules}=loadGame();const {engine,finance}=modules;const e=ctx.__ct_engine;ctx.__ct_ui.showSetup=false;e.g=engine.createInitialState({configured:true});e.g.selectedTab='ma';e.g.week=24;e.g.companyCash=200000000;e.g.publicCompany=true;e.g.stockPrice=1000;e.g.sharesOut=1000000;e.g.finance=finance.defaultFinanceState(e.g);e.g.acquisitionTargets=[{id:'t1',name:'UI承認テック',valuation:68400000,sales:100000000,operatingProfit:12000000,growth:.1,risk:.1,synergy:.1,activeDealID:'d1'}];e.g.maDealRooms=[{id:'d1',targetID:'t1',status:'accepted',diligenceLevel:'full',diligenceConfidence:.94,acceptedTerms:{method:'friendly',finalPrice:90000000,acceptedWeek:23,closingDeadlineWeek:28,offerRound:1},history:[]}];const beforeCash=e.g.companyCash,beforeWeek=e.g.week,beforeTxn=e.g.finance.transactions.length,beforeApproval=JSON.stringify(e.g.maDealRooms[0].boardApproval||null);e.emit();const html=ctx.document.getElementById('app').innerHTML;assert.equal(e.g.companyCash,beforeCash);assert.equal(e.g.week,beforeWeek);assert.equal(e.g.finance.transactions.length,beforeTxn);assert.equal(JSON.stringify(e.g.maDealRooms[0].boardApproval||null),beforeApproval);for(const sel of ['data-ma-board-memo','data-ma-board-decision','data-ma-board-blockers','data-ma-board-warnings','data-ma-board-positives','data-ma-board-approve','data-ma-board-approval-status','data-ma-board-expiry','data-ma-closing-readiness'])assert.ok(html.includes(sel),sel);assert.ok(html.includes('disabled'),'unapproved close disabled');e.approveMAClosing('d1');e.emit();const html2=ctx.document.getElementById('app').innerHTML;assert.ok(html2.includes('data-ma-board-revoke'));assert.ok(html2.includes('承認済み'));console.log('ma board approval ui ok');

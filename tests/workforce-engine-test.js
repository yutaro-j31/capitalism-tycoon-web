const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.hasHeadOffice=true;e.g.officeCapacity=20;e.g.companyCash=100000000;e.g.departments.operations={id:'operations',name:'Ops',weeklyCost:120000};e.g.departmentStaff.operations=1;modules.workforce.migrateV7(e.g);let team=e.g.workforceTeams.find(t=>t.departmentID==='operations');team.headcount=3;modules.workforce.syncDepartmentStaff(e.g);modules.workforce.recompute(e.g);assert.equal(e.g.departmentStaff.operations,3);const r=e.g.workforceResultsByDepartmentID.operations;assert(r.availableCapacity>0);assert(Number.isFinite(r.utilization));const before=JSON.stringify(e.g);const v=modules.workforce.validate(e.g);assert.equal(v.ok,true,v.errors.join('\n'));assert.equal(JSON.stringify(e.g),before,'validate must be read-only');console.log('workforce engine ok');

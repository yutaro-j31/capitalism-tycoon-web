'use strict';
const {loadGame}=require('./harness');
const PRINCIPAL=Number(process.env.WORKING_CAPITAL||0);
const TERM=104,RATE=.07;
const SEEDS=Array.from({length:20},(_,i)=>190826100+i);
function lcg(seed){let s=seed>>>0;return()=>{s=(Math.imul(s,1664525)+1013904223)>>>0;return s/2**32;};}
function run(seed){const {ctx}=loadGame({random:lcg(seed),headless:true}),e=new ctx.__ct_headlessEngineClass();e.save=()=>{};e.emit=()=>{};e.configure({playerName:'Tester',companyName:'RE Co',difficulty:'normal',scenario:'free'});e.g.skipWeeklyValidation=true;e.g.seed=seed;const free=e.g.tenants.filter(t=>!t.occupiedBy).sort((a,b)=>b.traffic-a.traffic||String(a.id).localeCompare(String(b.id))),t=free.find(x=>x.businessID==='realEstateAgency')||free[0];if(!e.openStore({tenantID:t.id,businessID:'realEstateAgency',name:'RE-1',operatingHours:3}))return{seed,opened:false};const weeklyPayment=PRINCIPAL>0?Math.floor(PRINCIPAL*(1+RATE*TERM/52)/TERM):0;e.g.companyCash+=PRINCIPAL;e.g.companyDebt+=PRINCIPAL;let balance=PRINCIPAL,minCash=e.g.companyCash,firstClose=null,closed=0;while(e.g.week<100&&!e.g.gameOver){e.advanceWeek(false);if(balance>0){const interest=Math.floor(balance*RATE/52),principalPay=Math.min(balance,Math.max(0,weeklyPayment-interest)),pay=interest+principalPay;if(e.g.companyCash>=pay){e.g.companyCash-=pay;balance-=principalPay;e.g.companyDebt=Math.max(0,e.g.companyDebt-principalPay);}}minCash=Math.min(minCash,e.g.companyCash);const row=e.g.stores[0]?.brokeragePipeline?.lastWeek;if(row?.closedDeals){closed+=row.closedDeals;if(firstClose===null)firstClose=e.g.week;}}return{seed,opened:true,survived:!e.g.gameOver,week:e.g.week,firstClose,closedDeals:closed,minCash,endingCash:e.g.companyCash,endingDebt:e.g.companyDebt,balance,gameOverReason:e.g.gameOverReason||null};}
const rows=SEEDS.map(run),survivors=rows.filter(r=>r.survived),sorted=survivors.map(r=>r.endingCash).sort((a,b)=>a-b),median=sorted.length?sorted[Math.floor(sorted.length/2)]:null;console.log('RE_WORKING_CAPITAL',JSON.stringify({principal:PRINCIPAL,term:TERM,rate:RATE,survivalRate:survivors.length/rows.length,survivors:survivors.length,total:rows.length,medianEndingCash:median,minEndingCash:sorted[0]??null,minCash:Math.min(...rows.map(r=>Number.isFinite(r.minCash)?r.minCash:Infinity)),firstCloseWeeks:rows.map(r=>r.firstClose),rows}));

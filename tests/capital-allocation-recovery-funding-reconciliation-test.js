const assert=require('node:assert');
const fs=require('node:fs');
const path=require('node:path');
const vm=require('node:vm');
const {loadGame}=require('./harness');
function install(load){
 for(const file of ['player-debt-service.js','treasury-prepayment.js','shareholder-returns.js','capital-allocation-score.js','capital-allocation-policy.js','capital-allocation-forecast.js','capital-allocation-actions.js','capital-allocation-decision-memo.js','capital-allocation-stress-test.js','capital-allocation-resilience-memo.js','capital-allocation-recovery-funding.js','capital-allocation-recovery-funding-options.js','capital-allocation-recovery-funding-readiness.js','capital-allocation-recovery-funding-reconciliation.js']){
  const key=file.replace('.js','').replace(/-([a-z])/g,(_,c)=>c.toUpperCase());
  if(!load.modules[key])vm.runInContext(fs.readFileSync(path.join(__dirname,'../js',file),'utf8'),load.ctx,{filename:file});
 }
 return load;
}
function publicGame(modules){
 const {engine,finance}=modules,state=engine.createInitialState({configured:true});state.week=27;state.publicCompany=true;state.selectedTab='market';state.companyCash=10_000_000;state.companyDebt=300_000_000;state.companyCredit=100;state.sharesOut=1_000_000;state.founderShares=600_000;state.stockPrice=100;state.ticker='CPTY';state.market=state.market.filter(row=>row.id!=='CPTY'&&row.id!=='EXT');state.market.push({id:'CPTY',name:state.companyName,sector:'コングロマリット',price:100,previous:100,dividendYield:0,volatility:0,trend:0,marketCap:100_000_000,per:20,pbr:2,issuedShares:1_000_000,dividendPerShare:0,shareholders:{},description:'test',listingMarket:'東証グロース',priceHistory:[{week:27,price:100}]},{id:'EXT',name:'外部上場株',sector:'IT',price:10_000,previous:10_000,dividendYield:0,volatility:0,trend:0,marketCap:10_000_000_000,per:20,pbr:2,issuedShares:1_000_000,dividendPerShare:0,shareholders:{},description:'test asset',listingMarket:'東証プライム',priceHistory:[{week:27,price:10_000}]});state.companyStocks={EXT:{qty:10_000,avg:8_000}};const property=state.properties[0];property.owner='company';property.value=800_000_000;property.price=800_000_000;property.purchasePrice=600_000_000;property.bookValue=600_000_000;state.finance=finance.defaultFinanceState(state);state.finance.capitalAllocationPolicy={id:'balanced',lastChangedWeek:-999,lastEvaluatedWeek:-1,executionScore:50,history:[]};finance.event(state,'revenue',100_000_000,{week:27,cashEffect:0,profitEffect:100_000_000,receivableAmount:100_000_000,sourceType:'fundingReconciliationFixture',sourceID:'baseline',idempotencyKey:'funding-reconciliation-revenue',description:'売掛売上'});state.finance.loans=[{id:'test-loan',loanID:'test-loan',status:'active',principal:300_000_000,outstandingPrincipal:300_000_000,interestRate:.1,maturityWeek:104}];finance.rebuildSnapshotForWeek(state,27);return new engine.TycoonEngine(state);
}
function syntheticSnapshot(game){const property=game.g.properties.find(row=>row.owner==='company');return{fingerprint:'manual-fixture',week:game.g.week,targetScore:70,optionId:'recommended',optionName:'現行推奨',targetPolicy:'growth',currentPolicy:'balanced',baselineCash:game.g.companyCash,baselineDebt:game.g.companyDebt,assetFunding:150_000_000,borrowing:50_000_000,sources:[{id:'stock:EXT',kind:'securities',sourceId:'EXT',units:1_000,amount:50_000_000,price:game.stock('EXT').price,holdingUnits:game.g.companyStocks.EXT.qty,averageCost:game.g.companyStocks.EXT.avg},{id:`property:${property.id}`,kind:'properties',sourceId:property.id,units:1,amount:100_000_000,owner:'company',marketValue:property.value,landCarryingValue:property.purchasePrice,buildingCarryingValue:0,buildingAssetCount:0}],steps:[{id:'policy',kind:'policy',label:'方針変更',order:1,amount:0},{id:'stock:EXT',kind:'securities',sourceId:'EXT',label:'株式売却',order:2,units:1_000,amount:50_000_000},{id:`property:${property.id}`,kind:'properties',sourceId:property.id,label:'不動産売却',order:3,units:1,amount:100_000_000},{id:'borrowing',kind:'borrowing',label:'会社借入',order:4,units:1,amount:50_000_000}]};}
const load=install(loadGame()),modules=load.modules,mod=modules.capitalAllocationRecoveryFundingReconciliation;
assert.ok(mod?.__installed);assert.equal(modules.engine.SAVE_KEY,'capitalism_tycoon_web_v1');assert.equal(modules.engine.SAVE_VERSION,9);
const game=publicGame(modules),snapshot=syntheticSnapshot(game),before=JSON.stringify(game.g),initial=game.capitalAllocationRecoveryFundingReconciliation(snapshot);
assert.equal(initial.status,'notStarted');assert.equal(initial.counts.pending,4);assert.equal(initial.completedCount,0);assert.equal(initial.progress,0);assert.equal(JSON.stringify(game.g),before,'reconciliation must remain read-only');
game.g.finance.capitalAllocationPolicy.id='growth';game.g.companyStocks.EXT.qty=9_500;game.g.companyDebt=snapshot.baselineDebt+25_000_000;const partial=game.capitalAllocationRecoveryFundingReconciliation(snapshot);assert.equal(partial.status,'inProgress');assert.equal(partial.steps.find(row=>row.kind==='policy').status,'complete');assert.equal(partial.steps.find(row=>row.kind==='securities').status,'partial');assert.equal(partial.steps.find(row=>row.kind==='properties').status,'pending');assert.equal(partial.steps.find(row=>row.kind==='borrowing').status,'partial');assert.ok(partial.progress>0&&partial.progress<1);
const property=game.g.properties.find(row=>row.owner==='company');game.g.companyStocks.EXT.qty=9_000;property.owner=null;game.g.companyDebt=snapshot.baselineDebt+snapshot.borrowing;game.g.companyCash=snapshot.baselineCash+snapshot.assetFunding+snapshot.borrowing;const complete=game.capitalAllocationRecoveryFundingReconciliation(snapshot);assert.equal(complete.status,'complete');assert.equal(complete.complete,true);assert.equal(complete.completedCount,4);assert.equal(complete.progress,1);assert.equal(complete.cashVariance,0);assert.equal(complete.debtVariance,0);
const divergedGame=publicGame(modules),divergedSnapshot=syntheticSnapshot(divergedGame),divergedProperty=divergedGame.g.properties.find(row=>row.owner==='company');divergedGame.g.companyStocks.EXT.qty=8_999;divergedProperty.owner='personal';divergedGame.g.companyDebt=divergedSnapshot.baselineDebt+divergedSnapshot.borrowing+1;const diverged=divergedGame.capitalAllocationRecoveryFundingReconciliation(divergedSnapshot);assert.equal(diverged.status,'diverged');assert.ok(diverged.steps.filter(row=>row.status==='diverged').length>=3);
const weekGame=publicGame(modules),weekSnapshot=syntheticSnapshot(weekGame);weekGame.g.week++;const weekResult=weekGame.capitalAllocationRecoveryFundingReconciliation(weekSnapshot);assert.equal(weekResult.status,'diverged');assert.ok(weekResult.issues.some(row=>row.id==='weekChanged'));assert.equal(weekResult.reason,'計画固定後に週が進行しました。');
const missing=publicGame(modules).capitalAllocationRecoveryFundingReconciliation();assert.equal(missing.status,'diverged');assert.ok(missing.issues.some(row=>row.id==='missingSnapshot'));
const pinGame=publicGame(modules),candidate=pinGame.capitalAllocationRecoveryFundingReadiness(50,'recommended');pinGame.g.finance.capitalAllocationPolicy.id=candidate.targetPolicy;pinGame.g.finance.capitalAllocationPolicy.lastChangedWeek=-999;const ready=pinGame.capitalAllocationRecoveryFundingReadiness(50,'recommended');assert.equal(ready.targetReached,true);assert.equal(ready.ready,true);const pinBefore=JSON.stringify(pinGame.g),pinned=pinGame.pinCapitalAllocationRecoveryFundingSnapshot(50,'recommended');assert.ok(pinned);assert.equal(JSON.stringify(pinGame.g),pinBefore,'pinning must not change game state');assert.equal(mod.pinned(pinGame).fingerprint,pinned.fingerprint);const pinnedResult=pinGame.capitalAllocationRecoveryFundingReconciliation();assert.ok(['notStarted','complete'].includes(pinnedResult.status));const pinnedHtml=mod.render(pinGame);assert.match(pinnedHtml,/data-capital-allocation-recovery-funding-reconciliation/);assert.match(pinnedHtml,/計画識別子/);assert.match(pinnedHtml,/固定を解除/);assert.equal(pinGame.clearCapitalAllocationRecoveryFundingSnapshot(),true);assert.equal(mod.pinned(pinGame),null);const emptyHtml=mod.render(pinGame);assert.match(emptyHtml,/現行推奨案を固定/);assert.match(emptyHtml,/Phase 8D-20/);
console.log('recovery funding post-execution reconciliation and transient pinning tests passed');

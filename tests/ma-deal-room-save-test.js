const assert=require('assert');const {load}=require('./ma-deal-room-test-helpers');const {engineModule,dr}=load();const {TycoonEngine,SAVE_KEY,SAVE_VERSION}=engineModule;const e=new TycoonEngine();e.g.configured=true;e.g.companyCash=1e9;e.g.departments.investment={established:true};e.g.acquisitionTargets=[{id:'legacy',name:'Legacy物流',domain:'物流',valuation:'100000000',sales:null,operatingProfit:8000000,growth:.1,risk:.2,synergy:.1,friendly:true,expiresWeek:999}];e.normalize();assert.equal(e.g.saveVersion,9);assert.equal(SAVE_KEY,'capitalism_tycoon_web_v1');assert.equal(SAVE_VERSION,9);assert.equal(e.g.acquisitionTargets[0].dealStatus,'available');assert(e.openMADealRoom('legacy'));const d=e.g.maDealRooms[0];assert(e.startMADueDiligence(d.id,'screening'));e.save();const raw=localStorage.getItem(SAVE_KEY);const loaded=TycoonEngine.load();assert.equal(loaded.g.maDealRooms[0].activeDiligence.scopeID,'screening');assert.equal(loaded.g.acquisitionTargets[0].activeDealID,d.id);loaded.exportSave();loaded.saveToSlot?.(1);loaded.loadFromSlot?.(1);const future=JSON.parse(raw);future.saveVersion=999;const migrated=engineModule.migrateSave(future);assert.equal(migrated.ok,false);assert.equal(migrated.version,999);console.log('ma deal room save tests passed');

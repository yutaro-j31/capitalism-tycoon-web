const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.finance=modules.finance.defaultFinanceState(e.g);e.g.stores.push({id:'s0',businessID:'ramen',prefID:'tokyo',name:'s0',status:'open',condition:100,operatingHours:3,openingWeek:1,weeksToOpen:0});modules.workforce.migrateV7(e.g);e.g.workforceCandidates.push({candidateID:'store-pay-a',roleID:'store-ops',storeID:'s0',targetStoreID:'s0',headcount:1,averageSkill:60,averageExperience:20,weeklySalaryPerPerson:21000,recruitmentFee:100,onboardingWeeks:1,expectedMorale:60,remoteCompatibility:0,expiresWeek:5,status:'open'},{candidateID:'store-pay-b',roleID:'store-ops',storeID:'s0',targetStoreID:'s0',headcount:2,averageSkill:60,averageExperience:20,weeklySalaryPerPerson:23000,recruitmentFee:100,onboardingWeeks:1,expectedMorale:60,remoteCompatibility:0,expiresWeek:5,status:'open'});e.hireWorkforceCandidate('store-pay-a');e.hireWorkforceCandidate('store-pay-b');const expected=21000+2*23000;assert.equal(modules.workforce.storeExtraPayroll(e.g,'s0'),expected);const before=e.g.companyCash;e.advanceWeek(false);const row=e.g.finance.transactions.find(t=>t.sourceType==='weekly-store-payroll'&&t.storeID==='s0');assert(row.amount>=52500+expected);assert.equal(modules.finance.validate(e.g).ok,true,modules.finance.validate(e.g).errors.join('\n'));const saved=JSON.parse(JSON.stringify(e.g));assert.equal(modules.workforce.storeExtraPayroll(saved,'s0'),expected);e.closeStore('s0');assert.equal(modules.workforce.storeExtraPayroll(e.g,'s0'),0);assert(e.g.companyCash<before+2000000);console.log('workforce store payroll ok');

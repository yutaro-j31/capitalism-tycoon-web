const assert=require('node:assert/strict');const {loadGame,findStateIssues}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.hasHeadOffice=false;e.g.stores=[];e.g.departments={};e.g.departmentStaff={};for(let i=0;i<4;i++)e.advanceWeek(false);assert.equal(e.g.workforceCandidates.length,0);assert.equal(e.g.workforceCandidates.filter(c=>c.roleID==='store-ops'&&!c.storeID&&!c.targetStoreID).length,0);assert.equal(modules.workforce.validate(e.g).ok,true,modules.workforce.validate(e.g).errors.join('\n'));assert.deepEqual(findStateIssues(e.g),[]);console.log('workforce empty start ok');

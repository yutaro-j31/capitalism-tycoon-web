const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame();const depts=modules.data.MASTER.departments;for(const d of depts){for(const hc of [1,2,5]){const s=modules.engine.createInitialState({configured:true});s.hasHeadOffice=true;s.departments[d.id]={...d,established:true};s.departmentStaff[d.id]=hc;modules.workforce.migrateV7(s);const oldCost=d.weeklyCost+Math.max(0,hc-1)*65000;const newCost=modules.workforce.weeklyPayroll(s);const diff=newCost-oldCost;const rate=Math.abs(diff)/Math.max(1,oldCost);assert(rate<=.01,`${d.id} hc${hc} old ${oldCost} new ${newCost} diff ${diff} rate ${rate}`);}}console.log('workforce payroll calibration ok');

const assert=require('assert');const fs=require('fs');const vm=require('vm');
const src=fs.readFileSync('js/real-estate-property-taxes.js','utf8'),ui=fs.readFileSync('js/real-estate-property-taxes-ui.js','utf8'),loader=fs.readFileSync('js/real-estate-maintenance-reserves.js','utf8');
assert(!src.includes('Math.random'));assert(!src.includes('Date.now'));assert(src.includes('HISTORY_LIMIT=500'));assert(src.includes('idempotencyKey'));assert(src.includes('lastPropertyTaxProcessedWeek'));assert(ui.includes('min-height:44px'));assert(loader.includes('real-estate-property-taxes.js'));assert(loader.includes('__capitalismTycoonRealEstatePropertyTaxesFailed'));
class Engine{constructor(g){this.g=g;}runTransaction(fn){return fn();}normalize(){return this.g;}}Engine.prototype.updateParityWeekly=function(){};const financeEvents=[];const context={globalThis:{__capitalismTycoonModules:{engine:{TycoonEngine:Engine},finance:{event:(...x)=>financeEvents.push(x)},realEstateMaintenanceReserves:{ensure:g=>g}}}};vm.runInNewContext(src,context);const mod=context.globalThis.__capitalismTycoonModules.realEstatePropertyTaxes;assert.equal(Object.keys(mod.REGIMES).length,3);
const g={week:12,companyCash:1e7,personalCash:2e6,properties:[{id:'p1',name:'本社ビル',owner:'company',marketValue:1e8}]};const e=new Engine(g);e.normalize();assert(e.setPropertyTaxRegime('p1','standard'));mod.processWeek(e);assert(g.properties[0].propertyTaxAccrued>0);const accrued=g.properties[0].propertyTaxAccrued;assert.equal(mod.processWeek(e).length,0);const paid=e.payPropertyTax('p1');assert.equal(paid,accrued);assert.equal(g.properties[0].propertyTaxAccrued,0);assert.equal(e.payPropertyTax('p1'),false);const saved=JSON.parse(JSON.stringify(g));assert(Number.isFinite(saved.properties[0].propertyTaxPaidTotal));for(let i=0;i<520;i++)mod.ensure(g).propertyTaxHistory.push({i});mod.ensure(g);assert.equal(g.propertyTaxHistory.length,500);assert(financeEvents.length>=2);console.log('real-estate-property-taxes: ok');
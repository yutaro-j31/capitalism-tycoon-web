const assert=require('node:assert/strict');const fs=require('node:fs');const app=fs.readFileSync('js/app.js','utf8');for(const a of ['hire-workforce-candidate','start-workforce-training','start-workforce-project','workforce-project-status','workforce-project-priority'])assert(app.includes(a),`${a} missing`);assert(!app.includes('w.recompute(g);const k='),'render must not recompute');console.log('workforce ui actions ok');

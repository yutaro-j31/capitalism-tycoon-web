const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame({random:()=>.5});const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.companyCash=100000000;e.g.stores.push({id:'s0',businessID:'ramen',prefID:'tokyo',name:'s0',status:'open',condition:100,operatingHours:3,openingWeek:1,weeksToOpen:0});e.g.finance=modules.finance.defaultFinanceState(e.g);modules.workforce.migrateV7(e.g);e.g.workforceCandidates.push({candidateID:'store-close',roleID:'store-ops',storeID:'s0',targetStoreID:'s0',headcount:2,averageSkill:60,averageExperience:20,weeklySalaryPerPerson:22000,recruitmentFee:100,onboardingWeeks:2,expectedMorale:60,remoteCompatibility:0,expiresWeek:5,status:'open'});e.hireWorkforceCandidate('store-close');assert(modules.workforce.storeExtraPayroll(e.g,'s0')>0);e.closeStore('s0');assert.equal(modules.workforce.storeExtraPayroll(e.g,'s0'),0);assert.equal(e.g.workforceTeams.filter(t=>t.storeID==='s0'&&t.status!=='closed'&&t.status!=='removed').length,0);assert.equal(modules.workforce.validate(e.g).ok,true,modules.workforce.validate(e.g).errors.join('\n'));assert.equal(modules.finance.validate(e.g).ok,true,modules.finance.validate(e.g).errors.join('\n'));assert.equal(modules.supply.validate(e.g).ok,true,modules.supply.validate(e.g).errors.join('\n'));console.log('workforce store close ok');

const assert=require('node:assert/strict');const {loadGame}=require('./harness');const {modules}=loadGame();const e=new modules.engine.TycoonEngine();e.g.configured=true;e.g.hasHeadOffice=true;e.g.officeCapacity=100;e.g.companyCash=100000000;e.g.departments.operations={...modules.data.MASTER.departments.find(d=>d.id==='operations'),established:true};e.g.departmentStaff.operations=1;modules.workforce.migrateV7(e.g);modules.workforce.addDepartmentHeadcount(e.g,'operations',2,{roleID:'procurement',weeklySalaryPerPerson:68000,onboardingWeeks:1,averageSkill:70,averageExperience:40,expectedMorale:60});const teams=e.g.workforceTeams.filter(t=>t.departmentID==='operations'&&!t.storeID);assert(teams.some(t=>t.roleID==='operations'));assert(teams.some(t=>t.roleID==='procurement'));assert.equal(e.g.departmentStaff.operations,teams.reduce((a,t)=>a+t.headcount,0));modules.workforce.recompute(e.g);assert(e.g.workforceResultsByDepartmentID.operations.requiredWorkload>=0);console.log('workforce role cohort ok');

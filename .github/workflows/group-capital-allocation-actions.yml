name: Group Capital Allocation Actions

on:
  pull_request:
    paths:
      - 'js/group-capital-allocation-actions.js'
      - 'tests/group-capital-allocation-actions-test.js'
      - 'play.html'
      - 'tests/run-all.js'
      - '.github/workflows/group-capital-allocation-actions.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --check js/group-capital-allocation-actions.js
      - run: node tests/group-capital-allocation-actions-test.js

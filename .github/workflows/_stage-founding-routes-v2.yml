name: Stage founding routes complete v2
on:
  push:
    branches: [feature/founding-routes-complete-v2]
permissions:
  contents: write
jobs:
  stage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/founding-routes-complete-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply audited implementation
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def read(path): return Path(path).read_text()
          def write(path, text):
              p=Path(path); p.parent.mkdir(parents=True, exist_ok=True); p.write_text(text)
          def one(text, old, new, label):
              if text.count(old)!=1:
                  raise SystemExit(f'{label}: expected one match, got {text.count(old)}')
              return text.replace(old,new,1)
          def between(text, start, end, replacement, label):
              i=text.find(start)
              if i<0: raise SystemExit(f'{label}: start marker missing')
              j=text.find(end,i)
              if j<0: raise SystemExit(f'{label}: end marker missing')
              return text[:i]+replacement+text[j:]

          # CSS + extracted baseline must remain byte-identical.
          css=read('css/app.css')
          css_add='''\n.founding-routes{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin:0 0 18px}.founding-route,.store-design-card,.investment-route-card,.decision-card,.capital-lock{background:var(--panel,#111b2d);border:1px solid var(--line,#2b3950);border-radius:16px;padding:16px}.founding-route h2,.store-design-card h2,.investment-route-card h2{margin-top:0}.founding-route .btn,.store-design-card .btn,.investment-route-card .btn,.decision-card .btn{min-height:44px}.operations-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.operations-metric{border:1px solid var(--line,#2b3950);border-radius:12px;padding:12px;min-width:0}.operations-bottleneck{border:2px solid #f59e0b;background:rgba(245,158,11,.08)}.store-design-card{margin:0 0 14px}.store-design-summary,.area-tags,.decision-card-actions{display:flex;flex-wrap:wrap;gap:8px}.store-design-summary>*{padding:5px 8px;border-radius:9px;background:rgba(255,255,255,.06)}.area-tags{margin-top:8px;font-size:12px}.area-tags span{padding:4px 7px;border-radius:999px;background:rgba(255,255,255,.06)}.store-policy-grid,.investment-order-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.store-policy-grid label,.investment-order-grid label{display:flex;flex-direction:column;gap:5px;min-width:0}.store-policy-grid select,.investment-order-grid select,.investment-order-grid input,.investment-route-card select{min-height:44px;max-width:100%}.decision-cards{display:grid;gap:10px;margin:0 0 16px}.decision-card{border-radius:13px}.decision-card h3{margin:0 0 5px}.decision-card p{margin:0 0 8px}.capital-lock{margin:0 0 14px}.capital-lock.locked{opacity:.84}.weekly-causal-list{display:grid;gap:6px;padding:0;list-style:none}.weekly-causal-list li{display:flex;justify-content:space-between;gap:10px;border-bottom:1px solid var(--line);padding:7px 0}.weekly-recommendation{font-weight:700}\n@media(max-width:700px){.founding-routes{grid-template-columns:1fr}.operations-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.inline-form{align-items:stretch;flex-direction:column}.inline-form select,.inline-form .btn{width:100%;min-height:44px}.modal{max-height:calc(100dvh - env(safe-area-inset-top) - env(safe-area-inset-bottom) - 24px);overflow:auto}.store-policy-grid,.investment-order-grid{grid-template-columns:1fr}.store-design-card,.investment-route-card,.decision-card,.capital-lock{padding:13px}}\n@media(max-width:380px){.operations-grid{grid-template-columns:1fr}}\n'''
          if '.founding-routes{' not in css:
              css=css.rstrip()+css_add
          write('css/app.css',css)
          write('tests/fixtures/extracted-css-baseline.css',css)

          write('docs/LOAD_ORDER.md','''# Runtime logical load groups\n\nThe classic script tags remain intentionally unbundled. Their logical ownership is:\n\n1. boot/save/economy/accounting (`runtime`, `data`, `finance`, `engine`, save storage)\n2. ramen/market/supply/workforce/report (`market`, `supply`, `workforce`, weekly reporting)\n3. initial personal/company listed-stock investing (`engine` and market UI)\n4. office-gated VC/capital policy/M&A/subsidiary (`ma-*`, startup and capital-allocation modules)\n5. multi-store/brand/HQ/vertical integration/IPO (`expansion`, office and listing modules)\n\nKeep dependency order explicit in `index.html` and `play.html`; new functionality should extend these groups rather than introduce a second simulation or a bundle.\n''')

          # app.js: two founding routes, start-time company stock access, D-UI opening fallback, causal report UI.
          app=read('js/app.js')
          app=one(app,
          "  const maActionLabel=ma.nextAction?.label||'買収候補探索';\n  return `<section class=\"ceo-dashboard\" aria-labelledby=\"ceo-dashboard-title\" data-ceo-dashboard=\"1\">",
          "  const maActionLabel=ma.nextAction?.label||'買収候補探索';\n  const companyPortfolio=engine.portfolioSummary('company');\n  const foundingRoutes=`<section class=\"founding-routes\" aria-label=\"創業ルート\"><article class=\"founding-route\"><h2>🍜 ラーメン経営</h2><p><strong>今週の最優先：</strong>物件を探す → ramen店出店</p><div class=\"kpi-grid mini\">${stat('客数',g.marketResultsByBusinessID?.ramen?.unitsSold||0)}${stat('利益',compactYen(g.stores.filter(s=>s.businessID==='ramen').reduce((a,s)=>a+finite(s.lastProfit),0)))}${stat('欠品',g.supplyResultsByBusinessID?.ramen?.stockoutUnits||0)}${stat('評判',g.businesses.find(b=>b.id==='ramen')?.brand||0)}</div><div class=\"button-row\"><button class=\"btn primary\" data-action=\"tab\" data-tab=\"map\">物件を探す</button><button class=\"btn secondary\" data-action=\"tab\" data-tab=\"business\">店舗を改善</button></div></article><article class=\"founding-route\"><h2>📈 投資・VC経営</h2><p><strong>今週の最優先：</strong>上場株ポートフォリオ作成 → オフィス契約</p><div class=\"kpi-grid mini\">${stat('現金',compactYen(g.companyCash))}${stat('投資資産',compactYen(companyPortfolio.marketValue))}${stat('評価損益',compactYen(companyPortfolio.unrealizedPL))}${stat('配当',compactYen(companyPortfolio.dividends))}${stat('集中リスク',pct(companyPortfolio.concentration))}</div><div class=\"button-row\"><button class=\"btn primary\" data-action=\"tab\" data-tab=\"market\">上場株を見る</button><button class=\"btn secondary\" data-action=\"tab\" data-tab=\"map\">オフィスを探す</button></div><small>${g.hasHeadOffice?'VC・M&A利用可能':'VC・M&Aはオフィス契約後に利用可能'}</small></article></section>`;\n  return `${foundingRoutes}<section class=\"ceo-dashboard\" aria-labelledby=\"ceo-dashboard-title\" data-ceo-dashboard=\"1\">",
          'app founding routes')
          old="function stockTradeCapacity(stock){const g=engine.g;const company=ui.selectedAccount==='company';const cash=finite(company?g.companyCash:g.personalCash);const unit=finite(stock?.price)*1.001;const blocked=company&&!g.departments.investment;return{company,cash,blocked,maxQty:blocked||unit<=0?0:Math.max(0,Math.floor(cash/unit))};}"
          new="function stockTradeCapacity(stock){const g=engine.g;const company=ui.selectedAccount==='company';const cash=finite(company?g.companyCash:g.personalCash);const quote=engine.stockExecution(stock,1,'buy'),unit=finite(quote?.total);return{company,cash,blocked:false,maxQty:unit<=0?0:Math.max(0,Math.floor(cash/unit))};}"
          app=one(app,old,new,'stock trade capacity')
          old="    case 'open-store':{const businessID=$(`#business-${id}`).value;askText('新店舗','店舗名',`${engine.g.companyName} ${engine.g.stores.length+1}号店`,name=>engine.openStore({tenantID:id,businessID,name,operatingHours:3}));break;}"
          new="    case 'open-store':{const select=document.getElementById(`business-${id}`),tenant=engine.g.tenants.find(t=>t.id===id),businessID=select?.value||tenant?.businessID;if(!businessID){engine.fail('出店する業態を選択してください。');break;}askText('新店舗','店舗名',`${engine.g.companyName} ${engine.g.stores.length+1}号店`,name=>engine.openStore({tenantID:id,businessID,name,operatingHours:3}));break;}"
          app=one(app,old,new,'open-store D-UI fallback')
          old="<h3>主なニュース</h3>${summary.newNews.map(n=>`<div class=\"news-line\">${esc(n)}</div>`).join('')||empty('新しいニュースはありません。')}"
          new="${summary.causalDrivers?.length?`<section class=\"weekly-impact-section\"><h3>${engine.g.stores.length?'今週の利益を動かした要因':'今週の投資ポートフォリオを動かした要因'}</h3><ul class=\"weekly-causal-list\">${summary.causalDrivers.map(x=>`<li><span>${esc(x.label)}</span><strong>${compactYen(x.amount)}</strong></li>`).join('')}</ul><p class=\"weekly-recommendation\">次週の推奨：${esc(summary.recommendation||'現金余力を確認する')}</p></section>`:''}<h3>主なニュース</h3>${summary.newNews.map(n=>`<div class=\"news-line\">${esc(n)}</div>`).join('')||empty('新しいニュースはありません。')}"
          app=one(app,old,new,'weekly causal UI')
          write('js/app.js',app)

          # engine.js audited core.
          eng=read('js/engine.js')
          eng=one(eng,
          "    personalStocks: {}, companyStocks: {}, favoriteStockIds: [], realizedCompanyStockPL: 0, realizedPersonalStockPL: 0,\n    subsidiaries:",
          "    personalStocks: {}, companyStocks: {}, favoriteStockIds: [], realizedCompanyStockPL: 0, realizedPersonalStockPL: 0,\n    personalStockDividends: 0, companyStockDividends: 0, pendingStockOrders: [], nextStockOrderSeq: 1,\n    investmentPolicy: 'stable-dividend', founderFundingLedger: [], founderContributedEquity: 0, founderShareholderLoan: 0, foundingMilestoneCompleted: false, lastCompanyPortfolioValue: 0,\n    subsidiaries:",
          'engine defaults')

          open_block=r'''  openStore({tenantID,businessID,name,operatingHours=3}) {
    const tenant = this.g.tenants.find(t=>t.id===tenantID);
    const business = this.business(businessID);
    if (!tenant) return this.fail('物件IDが不正です。');
    if (tenant.occupiedBy) return this.fail('選択したテナントはすでに契約済みです。');
    if (!business) return this.fail('業種が見つかりません。');
    const cost = business.storeCost + tenant.deposit;
    if (this.g.companyCash < cost) return this.fail(`出店には${yen(cost)}が必要です。`);
    const before=deepClone(this.g);
    try {
      this.g.companyCash -= cost; tenant.occupiedBy = 'player';
      const weeks = business.storeCost >= 15_000_000 ? 8 : business.storeCost >= 7_000_000 ? 5 : 3;
      const store = {id:uuid(),businessID,prefID:tenant.prefID,name:name||`${this.g.companyName} ${this.g.stores.length+1}号店`,openedWeek:this.g.week,
        quality:business.quality,brand:business.brand,condition:100,lastSales:0,lastProfit:0,status:'preparing',openingWeek:this.g.week+weeks,weeksToOpen:weeks,
        tenantID,cityName:tenant.cityName,operatingHours:Number(operatingHours)};
      if(businessID==='ramen'){store.concept='station-tonkotsu';store.signatureMenu='classic-value';store.rebrandUntilWeek=0;store.manager=null;store.delegationPolicy={price:'standard',procurement:'standard',staffing:'standard'};}
      this.g.stores.push(store);
      finance.addFixedAsset(this.g,{assetID:`store-${store.id}`,assetType:'storeEquipment',acquisitionCost:business.storeCost,usefulLifeWeeks:260,salvageValue:business.storeCost*.1,businessID,storeID:store.id});
      finance.event(this.g,'capitalExpenditure',business.storeCost,{cashEffect:-business.storeCost,assetEffect:business.storeCost,businessID,storeID:store.id,sourceType:'openStore',sourceID:store.id,description:`${store.name} 店舗設備`});
      finance.event(this.g,'otherInvesting',tenant.deposit,{cashEffect:-tenant.deposit,assetEffect:tenant.deposit,businessID,storeID:store.id,sourceType:'openStoreDeposit',sourceID:store.id,description:`${store.name} 保証金`});
      this.notify(`${store.name}の出店準備を開始しました。開店まで${weeks}週。`,'success');
      this.completeFoundingMilestone('store');this.evaluateProgression();this.save();this.emit();return true;
    } catch(error) {
      this.g=before;
      return this.fail(`出店処理を完了できませんでした: ${error?.message||'不明なエラー'}`);
    }
  }

  setRamenStrategy(storeID,{concept,signatureMenu}={}) {
    const store=this.g.stores.find(s=>s.id===storeID&&s.businessID==='ramen');if(!store)return this.fail('対象のラーメン店舗が見つかりません。');
    const concepts=['station-tonkotsu','artisan-shoyu','family-miso','late-rich'],menus=['classic-value','rich-quality','limited-topping','set-refill'];
    if(concept&&!concepts.includes(concept))return this.fail('店舗コンセプトが不正です。');if(signatureMenu&&!menus.includes(signatureMenu))return this.fail('看板メニュー方針が不正です。');
    const changing=Boolean((concept&&store.concept&&concept!==store.concept)||(signatureMenu&&store.signatureMenu&&signatureMenu!==store.signatureMenu));
    if(changing){const fee=300_000;if(this.g.companyCash<fee)return this.fail('再ブランディング費用30万円が不足しています。');this.g.companyCash-=fee;finance.event(this.g,'advertising',fee,{cashEffect:-fee,profitEffect:-fee,businessID:'ramen',storeID:store.id,sourceType:'storeRebrand',sourceID:`${store.id}-${this.g.week}`,description:`${store.name} 再ブランディング`});store.rebrandUntilWeek=this.g.week+2;}
    if(concept)store.concept=concept;if(signatureMenu)store.signatureMenu=signatureMenu;this.save();this.emit();return true;
  }
  ramenModifiers(storeID){const s=this.g.stores.find(x=>x.id===storeID);const c={
    'station-tonkotsu':{demand:1.03,capacity:1.08,quality:.98,unitCost:1.01,supply:1.04},'artisan-shoyu':{demand:.98,capacity:.96,quality:1.08,unitCost:1.07,supply:1.02},
    'family-miso':{demand:1.06,capacity:1.02,quality:1.01,unitCost:1.01,supply:1},'late-rich':{demand:1.04,capacity:.98,quality:1.04,unitCost:1.05,supply:1.03}
  }[s?.concept]||{demand:1,capacity:1,quality:1,unitCost:1,supply:1};const m={
    'classic-value':{demand:1.03,capacity:1.03,quality:.99,unitCost:.96,supply:.98},'rich-quality':{demand:.99,capacity:.96,quality:1.06,unitCost:1.07,supply:1.04},
    'limited-topping':{demand:1.03,capacity:.98,quality:1.03,unitCost:1.04,supply:1.06},'set-refill':{demand:1.04,capacity:.97,quality:1,unitCost:1.02,supply:1.03}
  }[s?.signatureMenu]||{demand:1,capacity:1,quality:1,unitCost:1,supply:1};return Object.fromEntries(Object.keys(c).map(k=>[k,c[k]*m[k]]));}
  setStoreManager(storeID,manager,policy={}){const s=this.g.stores.find(x=>x.id===storeID&&x.businessID==='ramen');if(!s)return this.fail('対象店舗が見つかりません。');const abilities=['planning','service','costControl'];if(!manager||abilities.some(k=>!Number.isFinite(Number(manager[k]))))return this.fail('店長能力が不正です。');s.manager={name:String(manager.name||'店長'),planning:clamp(manager.planning,0,100),service:clamp(manager.service,0,100),costControl:clamp(manager.costControl,0,100),morale:clamp(finite(manager.morale,70),0,100),fatigue:clamp(finite(manager.fatigue,10),0,100)};s.delegationPolicy={price:policy.price||'standard',procurement:policy.procurement||'standard',staffing:policy.staffing||'standard'};this.save();this.emit();return true;}
  setStoreDelegation(storeID,kind,value){const s=this.g.stores.find(x=>x.id===storeID&&x.businessID==='ramen');if(!s?.manager)return this.fail('店長の配置が必要です。');const valid={price:['profit','standard','traffic'],procurement:['stable','standard','lean'],staffing:['service','standard','cost']};if(!valid[kind]?.includes(value))return this.fail('委任方針が不正です。');s.delegationPolicy=s.delegationPolicy||{price:'standard',procurement:'standard',staffing:'standard'};s.delegationPolicy[kind]=value;this.save();this.emit();return true;}
  applyManagerDelegation(){for(const s of this.g.stores.filter(x=>x.manager&&x.status==='open')){const m=s.manager,p=s.delegationPolicy||{},wf=this.g.workforceResultsByStoreID?.[s.id]||{};m.fatigue=clamp(finite(wf.fatigue,m.fatigue),0,100);m.morale=clamp(finite(wf.morale,m.morale),0,100);const effect=clamp((m.planning+m.service+m.costControl)/300*(.7+m.morale/250)*(1-m.fatigue/180),.05,1);const setting=this.g.supplySettingsByStoreID[s.id]||(this.g.supplySettingsByStoreID[s.id]={});const target=p.procurement==='stable'?1.5:p.procurement==='lean'?.6:1;setting.safetyStockWeeks=clamp(finite(setting.safetyStockWeeks,1)*(1-effect*.2)+target*effect*.2,.25,3);s.lastManagerExplanation=`${m.name}が段取り・接客・原価管理、士気${Math.round(m.morale)}、疲労${Math.round(m.fatigue)}を基に委任方針を適用しました。`;}}
  storeOperations(storeID){const s=this.g.stores.find(x=>x.id===storeID),m=this.g.marketResultsByStoreID?.[storeID]||s?.marketResult||{},w=this.g.workforceResultsByStoreID?.[storeID]||{},p=this.g.supplyResultsByStoreID?.[storeID]||{};const demand=finite(m.potentialDemand,finite(m.unitsSold)+finite(m.lostDemand)),served=finite(m.unitsSold),lost=finite(m.lostDemand),cap=finite(m.effectiveCapacity,served),fill=finite(p.fillRate,1),spoil=finite(p.spoilageUnits,p.spoilage),scores={demand:demand?served/demand:1,capacity:demand?cap/demand:1,ingredients:Math.min(fill,spoil>0?.7:1),reputation:clamp((finite(s?.quality,50)+finite(s?.brand,20)+finite(w.serviceQuality,50))/300,0,1)};const key=Object.entries(scores).sort((a,b)=>a[1]-b[1]||a[0].localeCompare(b[0]))[0][0];const reasons={demand:'需要に対して実来店が弱く、価格・商圏適合の見直しが必要です。',capacity:'需要が提供力を上回り、待ち時間による機会損失が発生しています。',ingredients:spoil>0?'廃棄が発生し、仕入量と販売量が合っていません。':'食材充足率が低く、欠品で販売機会を失っています。',reputation:'品質・価格・接客の総合評価が弱く、再来店につながっていません。'};return{demand:{demand,served,lost},capacity:{effective:cap,staffLimited:finite(w.staffLimitedCapacity,cap),queueRisk:demand?Math.max(0,(demand-cap)/demand):0},ingredients:{fillRate:fill,stockout:finite(p.stockoutUnits),spoilage:spoil},reputation:{quality:finite(s?.quality),price:finite(this.business(s?.businessID)?.price),service:finite(w.serviceQuality,50),loyalty:finite(s?.brand)},bottleneck:key,reason:reasons[key],suggestions:key==='ingredients'?['仕入方針を調整','販売予測を確認']:key==='capacity'?['人員配置を見直す','提供工程を改善']:['価格と品質を確認','店長方針を確認']};}

'''
          eng=between(eng,"  openStore({tenantID,businessID,name,operatingHours=3}) {","  closeStore(id) {",open_block,'open store region')

          stock_block=r'''  buyStock(stockID,qty,account='personal',opts={}) {
    const stock=this.stock(stockID);qty=Math.max(0,Math.floor(qty));if(!stock||qty<1)return this.fail('数量が不正です。');
    if(!['company','personal'].includes(account))return this.fail('投資口座が不正です。');const execution=this.stockExecution(stock,qty,'buy',opts);if(execution.filled<1)return this.fail('注文条件では約定しませんでした。');const cost=execution.total,filled=execution.filled,cashKey=account==='company'?'companyCash':'personalCash';
    if(this.g[cashKey]<cost)return this.fail('資金が不足しています。');
    this.g[cashKey]-=cost;if(account==='company')finance.event(this.g,'investmentPurchase',cost,{cashEffect:-cost,assetEffect:cost,sourceType:'buyStock',sourceID:`${stockID}-${this.g.week}`,description:`${stock.name} 株式購入`});const key=account==='company'?'companyStocks':'personalStocks';const h=this.g[key][stockID]||{qty:0,avg:0};
    h.avg=(h.avg*h.qty+cost)/(h.qty+filled);h.qty+=filled;this.g[key][stockID]=h;
    stock.price*=1+Math.min(.03,filled/Math.max(1,stock.issuedShares)*.6);stock.marketCap=stock.price*stock.issuedShares;if(account==='company')this.completeFoundingMilestone('investment');
    this.notify(`${account==='company'?'会社':'個人'}口座で${stock.name}を${filled.toLocaleString()}株購入しました。${filled<qty?'流動性により一部約定。':''}`,'success');this.save();this.emit();return true;
  }
  sellStock(stockID,qty,account='personal',opts={}) {
    const stock=this.stock(stockID);qty=Math.max(0,Math.floor(qty));if(!['company','personal'].includes(account))return this.fail('投資口座が不正です。');const key=account==='company'?'companyStocks':'personalStocks';const h=this.g[key][stockID];
    if(!stock||!h||qty<1||h.qty<1)return this.fail('売却可能株数を超えています。');const requested=Math.min(qty,h.qty),execution=this.stockExecution(stock,requested,'sell',opts);if(execution.filled<1)return this.fail('注文条件では約定しませんでした。');const filled=execution.filled,proceeds=execution.total,soldBook=h.avg*filled,profit=proceeds-soldBook;this.g[account==='company'?'companyCash':'personalCash']+=proceeds;if(account==='company')finance.event(this.g,'investmentSale',proceeds,{cashEffect:proceeds,assetEffect:-soldBook,profitEffect:profit,sourceType:'sellStock',sourceID:`${stockID}-${this.g.week}`,description:`${stock.name} 株式売却`});
    h.qty-=filled;if(h.qty===0)delete this.g[key][stockID];
    this.g[account==='company'?'realizedCompanyStockPL':'realizedPersonalStockPL']+=profit;
    stock.price*=1-Math.min(.03,filled/Math.max(1,stock.issuedShares)*.6);stock.marketCap=stock.price*stock.issuedShares;
    this.notify(`${stock.name}を${filled.toLocaleString()}株売却しました。損益${yen(profit)}。${filled<qty?'流動性により一部約定。':''}`,profit>=0?'success':'warning');this.save();this.emit();return true;
  }
  stockExecution(stock,qty,side,{limitPrice=null}={}){const requested=Math.max(0,Math.floor(finite(qty))),liquidity=Math.max(1000,Math.floor(finite(stock.issuedShares,1)*.002)),spread=clamp(.001+finite(stock.volatility,.05)*.02,.001,.006),price=finite(stock.price)*(side==='buy'?1+spread:1-spread);if(Number.isFinite(Number(limitPrice))&&Number(limitPrice)>0&&((side==='buy'&&price>Number(limitPrice))||(side==='sell'&&price<Number(limitPrice))))return{requested,filled:0,unfilled:requested,price,spread,commission:0,total:0,liquidity};const filled=Math.min(requested,liquidity),commission=filled?Math.max(50,price*filled*.001):0;return{requested,filled,unfilled:requested-filled,price,spread,commission,total:side==='buy'?price*filled+commission:Math.max(0,price*filled-commission),liquidity};}
  placeStockOrder({stockID,qty,account='personal',side='buy',type='market',limitPrice=null,expiresWeek=null}={}){const stock=this.stock(stockID);qty=Math.floor(finite(qty));if(!stock||qty<1||!['company','personal'].includes(account)||!['buy','sell'].includes(side)||!['market','limit'].includes(type))return this.fail('注文内容が不正です。');if(type==='market')return side==='buy'?this.buyStock(stockID,qty,account):this.sellStock(stockID,qty,account);limitPrice=finite(limitPrice);if(limitPrice<=0)return this.fail('指値価格が不正です。');const key=account==='company'?'companyStocks':'personalStocks',cash=account==='company'?this.g.companyCash:this.g.personalCash;if(side==='buy'&&cash<qty*limitPrice)return this.fail('資金が不足しています。');if(side==='sell'&&finite(this.g[key]?.[stockID]?.qty)<qty)return this.fail('売却可能株数を超えています。');this.g.pendingStockOrders.push({id:`stock-order-${this.g.nextStockOrderSeq++}`,stockID,qty,remaining:qty,account,side,type,status:'pending',limitPrice,createdWeek:this.g.week,expiresWeek:expiresWeek||this.g.week+4});this.save();this.emit();return true;}
  processStockOrders(){for(const o of this.g.pendingStockOrders.filter(x=>x.status==='pending'||x.status==='partial')){if(this.g.week>o.expiresWeek){o.status='expired';continue;}const stock=this.stock(o.stockID);if(!stock){o.status='expired';continue;}const execution=this.stockExecution(stock,o.remaining,o.side,{limitPrice:o.limitPrice});if(execution.filled<1)continue;const ok=o.side==='buy'?this.buyStock(o.stockID,execution.filled,o.account,{limitPrice:o.limitPrice}):this.sellStock(o.stockID,execution.filled,o.account,{limitPrice:o.limitPrice});if(ok){o.remaining-=execution.filled;o.status=o.remaining>0?'partial':'filled';}}}
  portfolioSummary(account='company'){const holdings=this.g[account==='company'?'companyStocks':'personalStocks'],cash=finite(this.g[account==='company'?'companyCash':'personalCash']),rows=Object.entries(holdings).map(([id,h])=>({id,value:finite(this.stock(id)?.price)*h.qty,cost:h.avg*h.qty})),marketValue=rows.reduce((a,x)=>a+x.value,0),costBasis=rows.reduce((a,x)=>a+x.cost,0),largest=Math.max(0,...rows.map(x=>x.value));return{marketValue,costBasis,unrealizedPL:marketValue-costBasis,realizedPL:finite(this.g[account==='company'?'realizedCompanyStockPL':'realizedPersonalStockPL']),dividends:finite(this.g[account==='company'?'companyStockDividends':'personalStockDividends']),cashRatio:cash/Math.max(1,cash+marketValue),concentration:largest/Math.max(1,marketValue)};}
  setInvestmentPolicy(policy){if(!['stable-dividend','growth','value-turnaround'].includes(policy))return this.fail('投資方針が不正です。');this.g.investmentPolicy=policy;this.save();this.emit();return true;}
  updateInvestmentRiskCredit(){const p=this.portfolioSummary('company'),prev=finite(this.g.lastCompanyPortfolioValue,p.marketValue);if(prev>0&&p.marketValue>0){const r=(p.marketValue-prev)/prev;if(r<-.05)this.g.companyCredit=clamp(this.g.companyCredit-Math.min(.5,Math.abs(r)*2),0,100);else if(r>.05)this.g.companyCredit=clamp(this.g.companyCredit+.05,0,100);}if(p.concentration>.65)this.g.companyCredit=clamp(this.g.companyCredit-.10,0,100);this.g.lastCompanyPortfolioValue=p.marketValue;return p;}
  founderFund(kind,amount){amount=finite(amount);if(!['equity','shareholder-loan'].includes(kind)||amount<=0)return this.fail('資金移動の種類または金額が不正です。');if(this.g.personalCash<amount)return this.fail('個人資金が不足しています。');this.g.personalCash-=amount;this.g.companyCash+=amount;const f=finance.ensureFinance(this.g);if(kind==='shareholder-loan'){this.g.companyDebt+=amount;this.g.founderShareholderLoan=finite(this.g.founderShareholderLoan)+amount;f.loans.push({loanID:`founder-loan-${this.g.week}-${f.loans.length+1}`,principal:amount,outstandingPrincipal:amount,interestRate:.02,termWeeks:260,remainingWeeks:260,repaymentMethod:'manual',weeklyPrincipalPayment:0,nextPaymentWeek:null,status:'active',lender:'founder'});finance.event(this.g,'debtBorrowing',amount,{cashEffect:amount,liabilityEffect:amount,sourceType:'founderFunding',sourceID:`loan-${this.g.week}-${this.g.founderFundingLedger.length}`,description:'創業者株主ローン'});}else{this.g.founderContributedEquity=finite(this.g.founderContributedEquity)+amount;f.balances.capitalSurplus=finite(f.balances.capitalSurplus)+amount;finance.event(this.g,'equityFinancing',amount,{cashEffect:amount,equityEffect:amount,sourceType:'founderFunding',sourceID:`equity-${this.g.week}-${this.g.founderFundingLedger.length}`,description:'創業者増資'});}this.g.founderFundingLedger.push({week:this.g.week,kind,amount});this.save();this.emit();return true;}
  repayFounderLoan(amount){amount=Math.min(finite(amount),finite(this.g.founderShareholderLoan));if(amount<=0)return this.fail('返済額が不正です。');if(this.g.companyCash<amount)return this.fail('会社現金が不足しています。');this.g.companyCash-=amount;this.g.personalCash+=amount;this.g.companyDebt-=amount;this.g.founderShareholderLoan-=amount;let remaining=amount;for(const loan of finance.ensureFinance(this.g).loans.filter(x=>x.status==='active'&&x.lender==='founder')){const pay=Math.min(remaining,finite(loan.outstandingPrincipal));loan.outstandingPrincipal-=pay;remaining-=pay;if(loan.outstandingPrincipal<=0){loan.outstandingPrincipal=0;loan.status='paid';}if(remaining<=0)break;}finance.event(this.g,'debtRepayment',amount,{cashEffect:-amount,liabilityEffect:-amount,sourceType:'founderLoanRepayment',sourceID:`${this.g.week}-${amount}`,description:'創業者株主ローン返済'});this.g.founderFundingLedger.push({week:this.g.week,kind:'shareholder-loan-repayment',amount});this.save();this.emit();return true;}
  completeFoundingMilestone(source){if(this.g.foundingMilestoneCompleted)return false;this.g.foundingMilestoneCompleted=true;if(!this.g.achievements.includes('home_founding'))this.g.achievements.push('home_founding');this.g.news.unshift(`第${this.g.week}週：実績「実家起業」を解除しました。`);return source;}
  districtTags(entity){const pref=this.g.prefs.find(p=>p.id===entity?.prefID)||{},area=this.area(pref.areaID||this.g.selectedArea)||{},traffic=finite(entity?.traffic,pref.traffic),rent=finite(entity?.rent,pref.rent),competition=finite(area.competition),wins=[];if(traffic>=1.05)wins.push('高い通行量');if(rent<finite(pref.rent,1)*1.05)wins.push('賃料効率');if(competition<.55)wins.push('競合余地');if(wins.length<2)wins.push('安定商圏','供給アクセス');const warning=competition>.65?'競合過密':rent>finite(pref.rent,1)*1.2?'高賃料':'採用難に注意';const recommended=traffic>1.12?'station-tonkotsu':competition<.45?'family-miso':rent>finite(pref.rent,1)*1.15?'artisan-shoyu':'late-rich';return{wins:wins.slice(0,2),warning,recommended};}
'''
          eng=between(eng,"  buyStock(stockID,qty,account='personal') {","  toggleFavorite(stockID) {",stock_block,'stock region')
          eng=one(eng,"    if(account==='company'&&!this.g.departments.investment)return this.fail('会社投資には投資部門が必要です。');","    if(account==='company'&&!this.g.hasHeadOffice)return this.fail('会社VC投資はオフィス契約後に利用可能です。');",'vc gate')
          eng=one(eng,"  makeSubsidiary(startupID) {\n    const s=this.g.startups.find(x=>x.id===startupID);if(!s||s.subsidiary)return false;","  makeSubsidiary(startupID) {\n    const s=this.g.startups.find(x=>x.id===startupID);if(!s||s.subsidiary)return false;\n    if(!this.g.hasHeadOffice)return this.fail('子会社化はオフィス契約後に利用可能です。');",'subsidiary gate')
          eng=one(eng,"  generateMATargets(force=false) {\n    if(!this.g.departments.investment)return this.fail('投資部門が必要です。');if(!force&&this.g.acquisitionTargets.length>=6)return true;","  generateMATargets(force=false) {\n    if(!this.g.hasHeadOffice)return this.fail('M&Aはオフィス契約後に利用可能です。');if(!force&&this.g.acquisitionTargets.length>=6)return true;",'ma gate')
          eng=one(eng,"    this.g.week++;this.g.month=Math.floor((this.g.week-1)/4)+1;if(this.g.week%52===0)this.g.founderAge++;","    this.g.week++;this.g.month=Math.floor((this.g.week-1)/4)+1;if(this.g.week%52===0)this.g.founderAge++;this.applyManagerDelegation();",'manager weekly')
          eng=one(eng,"this.updateMacro();this.updateMarket();this.updateProperties();","this.updateMacro();this.updateMarket();this.processStockOrders();this.updateInvestmentRiskCredit();this.updateProperties();",'order processing timing')
          # Quarter dividends: do not double-book finance; recordWeekly already emits investmentDividend for stockIncome.
          start="    if(this.g.week%13===0){for(const [id,h] of Object.entries(this.g.companyStocks))"
          end="    const projectRun=workforce.advanceProjects(this.g);"
          div_block="""    if(this.g.week%13===0){for(const [id,h] of Object.entries(this.g.companyStocks)){const s=this.stock(id);if(s&&id!==this.g.ticker)stockIncome+=h.qty*(s.dividendPerShare||s.price*s.dividendYield/4);}this.g.companyStockDividends=finite(this.g.companyStockDividends)+stockIncome;for(const [id,h] of Object.entries(this.g.personalStocks)){const s=this.stock(id);if(s&&id!==this.g.ticker){const personalDividend=h.qty*(s.dividendPerShare||s.price*s.dividendYield/4)*.797;this.g.personalCash+=personalDividend;this.g.personalStockDividends=finite(this.g.personalStockDividends)+personalDividend;}}if(this.g.publicCompany&&this.g.dividendPerShare>0){dividend=this.g.dividendPerShare*Math.max(0,this.g.sharesOut-this.g.treasuryBuybackShares);const founderGross=dividend*this.g.founderOwnershipRatio;this.g.personalCash+=founderGross*.797;expenses+=dividend;}}\n"""
          eng=between(eng,start,end,div_block,'dividend block')
          old="    const report={week:this.g.week,sales,expenses,rentIncome,stockIncome,interest,dividend,officeCost,spoilageExpense:finite(spoilage?.cost),profit,investmentPL:subs.profit,companyStockUnrealizedPL:this.unrealizedPL('company'),propertyDepreciation,tax};this.g.lastReport=report;this.g.reports.push(report);this.g.reports=this.g.reports.slice(-520);\n    this.recordHistory(sales,profit);this.evaluateProgression();this.generateRecurringEvents();"
          new="    const causal=this.g.stores.length?[{kind:'revenue',label:'店舗売上',amount:sales},{kind:'cost',label:'営業費用',amount:-expenses},{kind:'opportunity',label:'失注機会',amount:-Object.values(this.g.marketResultsByStoreID||{}).reduce((a,r)=>a+finite(r.lostDemand)*finite(r.price),0)}]:[{kind:'portfolio',label:'上場株評価損益',amount:this.unrealizedPL('company')},{kind:'dividend',label:'受取配当',amount:stockIncome},{kind:'liquidity',label:'会社現金',amount:this.g.companyCash}];causal.sort((a,b)=>Math.abs(b.amount)-Math.abs(a.amount)||a.label.localeCompare(b.label));\n    const report={week:this.g.week,sales,expenses,rentIncome,stockIncome,interest,dividend,officeCost,spoilageExpense:finite(spoilage?.cost),profit,investmentPL:subs.profit,companyStockUnrealizedPL:this.unrealizedPL('company'),propertyDepreciation,tax,causalDrivers:causal.slice(0,3),recommendation:this.g.stores.length?(causal.some(x=>x.kind==='opportunity'&&x.amount<0)?'欠品・提供力を改善する':'最も利益率の高い店舗へ集中する'):(this.portfolioSummary('company').concentration>.5?'集中投資リスクを下げる':'投資余力と配当を確認する')};this.g.lastReport=report;this.g.reports.push(report);this.g.reports=this.g.reports.slice(-520);\n    this.recordHistory(sales,profit);this.evaluateProgression();this.generateRecurringEvents();"
          eng=one(eng,old,new,'causal report')
          eng=one(eng,"    if(this.g.departments.investment&&this.g.acquisitionTargets.length<3&&this.g.week%8===0)this.generateMATargets(true);","    if(this.g.hasHeadOffice&&this.g.acquisitionTargets.length<3&&this.g.week%8===0)this.generateMATargets(true);",'recurring ma gate')
          write('js/engine.js',eng)

          # market.js: apply concept/menu/manager before allocation so revenue/units/cost stay coherent.
          market=read('js/market.js')
          market_fn=r'''function storeOffer(state,store){const b=(state.businesses||[]).find(x=>x.id===store.businessID)||{},p=(state.prefs||[]).find(x=>x.id===store.prefID)||{},tenant=(state.tenants||[]).find(t=>t.id===store.tenantID)||{},cm={'station-tonkotsu':{cap:1.08,q:.98,cost:1.01,conv:6,nov:1},'artisan-shoyu':{cap:.96,q:1.08,cost:1.07,conv:-1,nov:4},'family-miso':{cap:1.02,q:1.01,cost:1.01,conv:3,nov:0},'late-rich':{cap:.98,q:1.04,cost:1.05,conv:1,nov:8}}[store.concept]||{cap:1,q:1,cost:1,conv:0,nov:0},mm={'classic-value':{cap:1.03,q:.99,cost:.96,nov:-1},'rich-quality':{cap:.96,q:1.06,cost:1.07,nov:3},'limited-topping':{cap:.98,q:1.03,cost:1.04,nov:8},'set-refill':{cap:.97,q:1,cost:1.02,nov:2}}[store.signatureMenu]||{cap:1,q:1,cost:1,nov:0},mgr=store.manager||null,pol=store.delegationPolicy||{},morale=mgr?clamp(finite(mgr.morale,70)/100,.2,1):1,fatigue=mgr?clamp(finite(mgr.fatigue,10)/100,0,.9):0,managerFactor=mgr?morale*(1-fatigue*.55):0,managerCap=mgr?1+(finite(mgr.planning,50)-50)/450*managerFactor:1,managerQ=mgr?1+(finite(mgr.service,50)-50)/500*managerFactor:1,managerCost=mgr?1-(finite(mgr.costControl,50)-50)/650*managerFactor:1,policyCap=pol.staffing==='service'?1.04:pol.staffing==='cost'?.96:1,policyCost=(pol.staffing==='service'?1.025:pol.staffing==='cost'?.975:1)*(pol.procurement==='stable'?1.01:pol.procurement==='lean'?.985:1),policyAppeal=pol.price==='traffic'?4:pol.price==='profit'?-2:0;const dept=id=>state.departments&&state.departments[id]?(.35+finite(state.departmentStaff?.[id],1)*.08):0,cmo=finite(state.executives?.CMO?.skill,0),coo=finite(state.executives?.COO?.skill,0),ads=dept('marketing')*.04+cmo/2200,ops=dept('operations')*.03+coo/2400,convenience=clamp(40+finite(p.traffic,1)*18+finite(tenant.traffic,1)*14+finite(store.operatingHours,3)*7+cm.conv+policyAppeal,0,100),quality=clamp((finite(b.quality)+finite(store.quality)*.35+ops*100)*cm.q*mm.q*managerQ,0,100),brandAwareness=clamp(finite(b.brand)+finite(store.brand)*.35+finite(state.companyReputation,0)*.18+ads*100,0,100),brandTrust=clamp(brandAwareness*.65+quality*.25+finite(state.companyReputation,0)*.10,0,100),wf=globalThis.__capitalismTycoonModules?.workforce;baseCap=effectiveCapacity(store,b,p)*cm.cap*mm.cap*managerCap*policyCap;let wfAdj=wf&&wf.TARGET_BUSINESS_IDS.indexOf(store.businessID)>=0?wf.storeAdjustment(state,store,baseCap):null;const serviceQuality=clamp(45+finite(state.employeeAbility,50)*.30+finite(state.employeeSatisfaction,55)*.18+ops*100+(wfAdj?wfAdj.serviceQualityAdjustment*100:0)+(mgr?(finite(mgr.service,50)-50)*.12*managerFactor:0),0,100),novelty=clamp(finite(b.dx,0)*.65+finite(b.quality,0)*.2+ads*25+cm.nov+mm.nov,0,100),satisfaction=clamp(35+quality*.32+serviceQuality*.25+brandTrust*.16,0,100),repeatRate=clamp(.12+satisfaction/180+brandTrust/380,0,.88),variableCostPerUnit=finite(b.unitCost)*(1+quality/1000)*(1-Math.min(.22,finite(b.efficiency)/260))/(1+dept('operations')*.04)*cm.cost*mm.cost*managerCost*policyCost;return{id:store.id,kind:'player',store,business:b,pref:p,price:Math.max(1,finite(b.price,1)),quality,brandAwareness,brandTrust,convenience,serviceQuality,novelty,capacity:(wfAdj?wfAdj.staffLimitedCapacity:baseCap),workforceAdjustment:wfAdj,variableCostPerUnit,customerSatisfaction:satisfaction,repeatRate};}
'''
          market=between(market,'function storeOffer(state,store){','function competitorOffers(state,businessID,prefID){',market_fn,'market store offer')
          write('js/market.js',market)

          # M&A deal-room office capability gate.
          ma=read('js/ma-deal-room.js')
          ma=one(ma,"if(!this.g.departments.investment)return this.fail('投資部門が必要です。');if(t.activeDealID)","if(!this.g.hasHeadOffice)return this.fail('M&Aはオフィス契約後に利用可能です。');if(t.activeDealID)",'ma deal room gate')
          write('js/ma-deal-room.js',ma)

          # UI enhancer: no MutationObserver; uses existing registry after app render.
          compat=read('js/play-runtime-compat.js')
          ui=r'''

// Founding routes mobile UI bridge. Uses ui-enhancer-registry; no MutationObserver.
(function(){'use strict';const M=globalThis.__capitalismTycoonModules,R=M?.uiEnhancerRegistry;if(!M?.engine?.TycoonEngine||!R?.registerUIEnhancer||M.foundingRoutesCompleteUI)return;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),pct=v=>`${Math.round((Number(v)||0)*100)}%`,yen=v=>`${Math.round(Number(v)||0).toLocaleString('ja-JP')}円`;const concepts=[['station-tonkotsu','駅前・速攻豚骨'],['artisan-shoyu','職人醤油'],['family-miso','家族味噌食堂'],['late-rich','深夜こってり専門']],menus=[['classic-value','原価重視の定番'],['rich-quality','濃厚こだわり'],['limited-topping','限定トッピング'],['set-refill','セット/替え玉重視']],policyNames={'stable-dividend':'安定配当','growth':'成長株','value-turnaround':'バリュー/再生'};function engine(){return M.playerEngineBridge?.getEngine?.()||globalThis.__ct_engine||null}function goto(tab){const b=[...document.querySelectorAll('.d-source-tabs [data-action="tab"],#d-ui-dock [data-action="tab"]')].find(x=>x.dataset.tab===tab);b?.click()}function decisions(e){const g=e.g,out=[];for(const s of g.stores.filter(x=>x.businessID==='ramen'&&x.status==='open')){const o=e.storeOperations(s.id);if(o.bottleneck==='ingredients'&&o.ingredients.stockout>0)out.push({p:100,title:`${s.name} 欠品警報`,body:`欠品 ${Math.round(o.ingredients.stockout)}。${o.reason}`,tab:'business'});else if(o.bottleneck==='capacity'&&o.demand.lost>0)out.push({p:90,title:`${s.name} 提供力警報`,body:`取り逃し ${Math.round(o.demand.lost)}。${o.reason}`,tab:'business'});if(s.manager?.lastManagerExplanation||s.lastManagerExplanation)out.push({p:55,title:`${s.name} 店長提案`,body:s.lastManagerExplanation||s.manager.lastManagerExplanation,tab:'business'});}const tenant=g.tenants.find(t=>!t.occupiedBy&&t.businessID==='ramen');if(tenant)out.push({p:40,title:'成長機会：出店候補',body:`${tenant.name}を視察できます。`,tab:'map'});const startup=g.startups.find(s=>s.alive&&s.fundingOpen);if(startup)out.push({p:g.hasHeadOffice?70:25,title:'投資機会：VC案件',body:`${startup.name}が資金調達中。${g.hasHeadOffice?'出資を検討できます。':'オフィス契約後に会社VCを利用できます。'}`,tab:g.hasHeadOffice?'venture':'map'});for(const [id,h] of Object.entries(g.companyStocks)){const s=e.stock(id);if(s?.previous>0&&(s.price-s.previous)/s.previous<=-.08)out.push({p:95,title:`株価警報：${s.name}`,body:`前週比 ${pct((s.price-s.previous)/s.previous)}。保有 ${h.qty}株。`,tab:'market'});}return out.sort((a,b)=>b.p-a.p||a.title.localeCompare(b.title)).slice(0,3)}function enhance(){const e=engine();if(!e?.g?.configured)return;const g=e.g,screen=document.getElementById('screen')||document.querySelector('#app main')||document.getElementById('app');if(!screen)return;if(g.selectedTab==='home'&&!screen.querySelector('[data-decision-cards]')){const rows=decisions(e);if(rows.length)screen.insertAdjacentHTML('afterbegin',`<section class="decision-cards" data-decision-cards aria-label="今週の決定カード">${rows.map(x=>`<article class="decision-card"><h3>${esc(x.title)}</h3><p>${esc(x.body)}</p><div class="decision-card-actions"><button class="btn primary small" data-fr-goto="${esc(x.tab)}">確認する</button></div></article>`).join('')}</section>`)}if(g.selectedTab==='map'){for(const item of screen.querySelectorAll('.item')){const b=item.querySelector('[data-action="open-store"],[data-d-ui-open-store]');if(!b||item.querySelector('.area-tags'))continue;const id=b.dataset.id||b.dataset.dUiOpenStore,t=g.tenants.find(x=>x.id===id);if(!t)continue;const tag=e.districtTags(t),labels={'station-tonkotsu':'駅前・速攻豚骨','artisan-shoyu':'職人醤油','family-miso':'家族味噌食堂','late-rich':'深夜こってり専門'};item.insertAdjacentHTML('beforeend',`<div class="area-tags"><span>勝ち筋：${tag.wins.map(esc).join(' / ')}</span><span>警戒：${esc(tag.warning)}</span><span>おすすめ：${esc(labels[tag.recommended]||tag.recommended)}</span></div>`);}}if(g.selectedTab==='business'){for(const s of g.stores.filter(x=>x.businessID==='ramen')){if(screen.querySelector(`[data-store-design="${CSS.escape(s.id)}"]`))continue;const o=e.storeOperations(s.id),m=s.manager,p=s.delegationPolicy||{price:'standard',procurement:'standard',staffing:'standard'};screen.insertAdjacentHTML('afterbegin',`<section class="store-design-card" data-store-design="${esc(s.id)}"><h2>${esc(s.name)} — 店づくり</h2><div class="store-design-summary"><strong>${esc(concepts.find(x=>x[0]===s.concept)?.[1]||'未設定')}</strong><span>看板：${esc(menus.find(x=>x[0]===s.signatureMenu)?.[1]||'未設定')}</span>${s.rebrandUntilWeek>g.week?`<span>再ブランディング中：第${s.rebrandUntilWeek}週まで</span>`:''}</div><div class="operations-grid"><div class="operations-metric ${o.bottleneck==='demand'?'operations-bottleneck':''}"><strong>客数</strong><span>${Math.round(o.demand.served)} / 需要${Math.round(o.demand.demand)}・取り逃し${Math.round(o.demand.lost)}</span></div><div class="operations-metric ${o.bottleneck==='capacity'?'operations-bottleneck':''}"><strong>提供力</strong><span>能力${Math.round(o.capacity.effective)}・行列${pct(o.capacity.queueRisk)}</span></div><div class="operations-metric ${o.bottleneck==='ingredients'?'operations-bottleneck':''}"><strong>食材</strong><span>充足${pct(o.ingredients.fillRate)}・欠品${Math.round(o.ingredients.stockout)}・廃棄${Math.round(o.ingredients.spoilage)}</span></div><div class="operations-metric ${o.bottleneck==='reputation'?'operations-bottleneck':''}"><strong>評判</strong><span>品質${Math.round(o.reputation.quality)}・接客${Math.round(o.reputation.service)}</span></div></div><p><strong>今週のボトルネック：</strong>${esc(o.reason)} 改善候補：${o.suggestions.map(esc).join(' / ')}</p><div class="store-policy-grid"><label>コンセプト<select data-fr-concept="${esc(s.id)}">${concepts.map(([id,n])=>`<option value="${id}" ${id===s.concept?'selected':''}>${n}</option>`).join('')}</select></label><label>看板メニュー<select data-fr-menu="${esc(s.id)}">${menus.map(([id,n])=>`<option value="${id}" ${id===s.signatureMenu?'selected':''}>${n}</option>`).join('')}</select></label>${m?`<label>価格委任<select data-fr-delegation="${esc(s.id)}" data-kind="price"><option value="profit" ${p.price==='profit'?'selected':''}>利益優先</option><option value="standard" ${p.price==='standard'?'selected':''}>標準</option><option value="traffic" ${p.price==='traffic'?'selected':''}>集客優先</option></select></label><label>仕入委任<select data-fr-delegation="${esc(s.id)}" data-kind="procurement"><option value="stable" ${p.procurement==='stable'?'selected':''}>安定在庫</option><option value="standard" ${p.procurement==='standard'?'selected':''}>標準</option><option value="lean" ${p.procurement==='lean'?'selected':''}>在庫圧縮</option></select></label><label>人員委任<select data-fr-delegation="${esc(s.id)}" data-kind="staffing"><option value="service" ${p.staffing==='service'?'selected':''}>サービス優先</option><option value="standard" ${p.staffing==='standard'?'selected':''}>標準</option><option value="cost" ${p.staffing==='cost'?'selected':''}>人件費優先</option></select></label><span>店長 ${esc(m.name)}｜段取り${Math.round(m.planning)} 接客${Math.round(m.service)} 原価${Math.round(m.costControl)}｜士気${Math.round(m.morale)} 疲労${Math.round(m.fatigue)}</span>`:`<button class="btn secondary" data-fr-manager="${esc(s.id)}">店長を配置</button>`}</div></section>`);}}if(g.selectedTab==='market'&&!screen.querySelector('[data-investment-route]')){const account=document.querySelector('[data-bind="selectedAccount"]')?.value||'personal',p=e.portfolioSummary(account),stock=document.querySelector('[data-stock-trade-panel]')?.dataset.id||g.market[0]?.id;screen.insertAdjacentHTML('afterbegin',`<section class="investment-route-card" data-investment-route><h2>${account==='company'?'会社':'個人'}ポートフォリオ</h2><div class="kpi-grid mini"><div class="stat"><span>時価</span><strong>${yen(p.marketValue)}</strong></div><div class="stat"><span>評価損益</span><strong>${yen(p.unrealizedPL)}</strong></div><div class="stat"><span>実現損益</span><strong>${yen(p.realizedPL)}</strong></div><div class="stat"><span>配当</span><strong>${yen(p.dividends)}</strong></div><div class="stat"><span>現金比率</span><strong>${pct(p.cashRatio)}</strong></div><div class="stat"><span>集中リスク</span><strong>${pct(p.concentration)}</strong></div></div>${account==='company'?`<label>投資方針<select data-fr-investment-policy><option value="stable-dividend" ${g.investmentPolicy==='stable-dividend'?'selected':''}>安定配当</option><option value="growth" ${g.investmentPolicy==='growth'?'selected':''}>成長株</option><option value="value-turnaround" ${g.investmentPolicy==='value-turnaround'?'selected':''}>バリュー/再生</option></select></label>`:''}<h3>指値注文</h3><div class="investment-order-grid"><label>売買<select data-fr-order-side><option value="buy">買い</option><option value="sell">売り</option></select></label><label>数量<input data-fr-order-qty inputmode="numeric" value="100"></label><label>指値<input data-fr-order-limit inputmode="decimal" value="${Number(e.stock(stock)?.price||0).toFixed(0)}"></label></div><button class="btn secondary" data-fr-limit-order data-stock-id="${esc(stock||'')}">指値注文を出す</button><small>未約定・部分約定・期限切れは注文状態として保存されます。</small></section>`)}if((g.selectedTab==='venture'||g.selectedTab==='ma')&&!g.hasHeadOffice&&!screen.querySelector('[data-capital-lock]')){screen.insertAdjacentHTML('afterbegin','<section class="capital-lock locked" data-capital-lock><strong>🔒 高度な資本戦略</strong><p>オフィスを契約すると投資部門・VC出資・M&A・子会社化の入口が利用できます。</p><button class="btn primary" data-fr-goto="map">オフィスを探す</button></section>');}}
R.registerUIEnhancer({id:'founding-routes-complete-v2',enhance});document.addEventListener('click',ev=>{const e=engine();if(!e)return;const g=ev.target.closest('[data-fr-goto]');if(g){ev.preventDefault();goto(g.dataset.frGoto);return}const m=ev.target.closest('[data-fr-manager]');if(m){ev.preventDefault();e.setStoreManager(m.dataset.frManager,{name:'店長',planning:55,service:55,costControl:55,morale:70,fatigue:10});return}const o=ev.target.closest('[data-fr-limit-order]');if(o){ev.preventDefault();const root=o.closest('[data-investment-route]'),account=document.querySelector('[data-bind="selectedAccount"]')?.value||'personal';e.placeStockOrder({stockID:o.dataset.stockId,qty:Number(root.querySelector('[data-fr-order-qty]')?.value),account,side:root.querySelector('[data-fr-order-side]')?.value||'buy',type:'limit',limitPrice:Number(root.querySelector('[data-fr-order-limit]')?.value),expiresWeek:e.g.week+4});}},true);document.addEventListener('change',ev=>{const e=engine();if(!e)return;if(ev.target.matches('[data-fr-concept]'))e.setRamenStrategy(ev.target.dataset.frConcept,{concept:ev.target.value});if(ev.target.matches('[data-fr-menu]'))e.setRamenStrategy(ev.target.dataset.frMenu,{signatureMenu:ev.target.value});if(ev.target.matches('[data-fr-delegation]'))e.setStoreDelegation(ev.target.dataset.frDelegation,ev.target.dataset.kind,ev.target.value);if(ev.target.matches('[data-fr-investment-policy]'))e.setInvestmentPolicy(ev.target.value);},true);M.foundingRoutesCompleteUI=Object.freeze({__installed:true});R.runUIEnhancers?.();})();
'''
          if 'founding-routes-complete-v2' not in compat: compat=compat.rstrip()+ui
          write('js/play-runtime-compat.js',compat)

          # Tests updated for office capability and deterministic report schema.
          p='tests/finance-startup-subsidiary-test.js';t=read(p);t=one(t,"e.g.departments.investment={name:'投資部'};","e.g.departments.investment={name:'投資部'}; e.g.hasHeadOffice=true;",p);write(p,t)
          p='tests/venture-investment-outcome-test.js';t=read(p);t=one(t,"  engine.g.departments.investment = { level: 1 };","  engine.g.departments.investment = { level: 1 };\n  engine.g.hasHeadOffice = true;",p);t=one(t,"  const proceeds = listed.price * holding.qty * 0.999;","  const proceeds = engine.stockExecution(listed, holding.qty, 'sell').total;",p);write(p,t)
          p='tests/venture-investment-reachability-test.js';t=read(p);t=one(t,"  engine.g.departments.investment = { level: 1 };","  engine.g.departments.investment = { level: 1 };\n  engine.g.hasHeadOffice = true;",p);write(p,t)
          p='tests/transaction-regression-test.js';t=read(p);t=one(t,"    delete legacyWeeklySummary.scenario;","    delete legacyWeeklySummary.scenario;\n    delete legacyWeeklySummary.causalDrivers;\n    delete legacyWeeklySummary.recommendation;",p);t=one(t,"  return clone({\n    randomCalls,","  const legacyLastReport=clone(g.lastReport);if(legacyLastReport){delete legacyLastReport.causalDrivers;delete legacyLastReport.recommendation;}\n  const legacyReports=clone(g.reports).map(r=>{delete r.causalDrivers;delete r.recommendation;return r;});\n  return clone({\n    randomCalls,",p);t=one(t,"    lastReport: g.lastReport,","    lastReport: legacyLastReport,",p);t=one(t,"    reports: g.reports,","    reports: legacyReports,",p);write(p,t)

          test=r'''const assert=require('node:assert/strict');
const fs=require('node:fs');
const {loadGame}=require('./harness');
const game=()=>loadGame().ctx.__ct_engine;

// Tokyo ramen opening, strategy defaults, failure atomicity and save-compatible fields.
{
 const e=game(),t=e.g.tenants.find(x=>x.prefID==='tokyo'&&x.businessID==='ramen'),cash=e.g.companyCash,b=e.business('ramen');
 assert.match(t.name,/東京 駅前1階/);assert.equal(e.openStore({tenantID:t.id,businessID:'ramen',name:'駅前本店'}),true);
 const s=e.g.stores.at(-1);assert.equal(s.tenantID,t.id);assert.equal(t.occupiedBy,'player');assert.equal(e.g.companyCash,cash-b.storeCost-t.deposit+500_000);
 assert.equal(s.concept,'station-tonkotsu');assert.equal(s.signatureMenu,'classic-value');assert(e.g.news.some(n=>n.includes('駅前本店')));assert(e.g.finance.transactions.some(x=>x.sourceType==='openStoreDeposit'));
 const snapshot=JSON.stringify({cash:e.g.companyCash,stores:e.g.stores,occupied:t.occupiedBy,tx:e.g.finance.transactions,news:e.g.news});assert.equal(e.openStore({tenantID:t.id,businessID:'ramen'}),false);assert.equal(JSON.stringify({cash:e.g.companyCash,stores:e.g.stores,occupied:t.occupiedBy,tx:e.g.finance.transactions,news:e.g.news}),snapshot);
 e.g.companyCash+=1_000_000;assert.equal(e.setRamenStrategy(s.id,{concept:'artisan-shoyu',signatureMenu:'rich-quality'}),true);assert(e.ramenModifiers(s.id).quality>1);assert(e.ramenModifiers(s.id).unitCost>1);assert(s.rebrandUntilWeek>e.g.week);
 assert.equal(e.setStoreManager(s.id,{name:'山田',planning:80,service:70,costControl:60,morale:75,fatigue:12},{price:'profit',procurement:'stable',staffing:'service'}),true);assert.equal(s.manager.planning,80);assert.equal(e.setStoreDelegation(s.id,'price','traffic'),true);
 const reloaded=new (loadGame().engineModule.TycoonEngine)(JSON.parse(JSON.stringify(e.g)));assert.equal(reloaded.g.stores.at(-1).concept,'artisan-shoyu');assert.equal(reloaded.g.stores.at(-1).manager.service,70);
}
// Deterministic bottlenecks and district tags.
{
 const e=game();e.g.stores=[{id:'s',businessID:'ramen',prefID:'tokyo',quality:50,brand:50}];e.g.marketResultsByStoreID.s={potentialDemand:100,unitsSold:50,lostDemand:50,effectiveCapacity:40};e.g.workforceResultsByStoreID.s={staffLimitedCapacity:55,serviceQuality:60};e.g.supplyResultsByStoreID.s={fillRate:.95,stockoutUnits:0,spoilageUnits:0};assert.equal(e.storeOperations('s').bottleneck,'capacity');
 e.g.supplyResultsByStoreID.s={fillRate:.2,stockoutUnits:40,spoilageUnits:0};assert.equal(e.storeOperations('s').bottleneck,'ingredients');const t=e.g.tenants.find(x=>x.prefID==='tokyo');assert.deepEqual(e.districtTags(t),e.districtTags(t));assert.equal(e.districtTags(t).wins.length,2);
}
// Company/personal books, partial fills, limit lifecycle and zero-store investment route.
{
 const e=game(),stock=e.g.market[0];e.g.companyCash=500_000_000;e.g.personalCash=500_000_000;const q=e.stockExecution(stock,10_000_000,'buy');assert(q.filled<q.requested);const c=e.g.companyCash;assert.equal(e.buyStock(stock.id,10_000_000,'company'),true);assert.equal(e.g.companyStocks[stock.id].qty,q.filled);assert(Math.abs((c-e.g.companyCash)-q.total)<1);
 const personalBefore=e.g.personalCash;assert.equal(e.buyStock(stock.id,10,'personal'),true);assert(e.g.personalCash<personalBefore);assert(e.g.personalStocks[stock.id]);assert(e.g.companyStocks[stock.id]);assert.equal(e.g.stores.length,0);assert(e.g.foundingMilestoneCompleted);
 const limit=stock.price;assert.equal(e.placeStockOrder({stockID:stock.id,qty:1,account:'company',side:'buy',type:'limit',limitPrice:limit}),true);const noFill=e.g.pendingStockOrders.at(-1);e.processStockOrders();assert.equal(noFill.status,'pending','spread must not cross a buy limit above the limit price');
 const high=stock.price*10,liq=e.stockExecution(stock,10_000_000,'buy',{limitPrice:high}).filled;assert.equal(e.placeStockOrder({stockID:stock.id,qty:liq+5,account:'company',side:'buy',type:'limit',limitPrice:high}),true);const partial=e.g.pendingStockOrders.at(-1);e.processStockOrders();assert.equal(partial.status,'partial');assert.equal(partial.remaining,5);e.processStockOrders();assert.equal(partial.status,'filled');
 const company=e.portfolioSummary('company'),personal=e.portfolioSummary('personal');assert(company.marketValue>0&&personal.marketValue>0);assert(company.concentration>=0&&company.concentration<=1);
}
// Founder funding is an explicit equity/loan ledger and keeps finance balanced.
{
 const loaded=loadGame(),e=loaded.ctx.__ct_engine;e.g.personalCash=20_000_000;const pc=e.g.personalCash,cc=e.g.companyCash;assert(e.founderFund('equity',2_000_000));assert.equal(e.g.personalCash,pc-2_000_000);assert.equal(e.g.companyCash,cc+2_000_000);assert.equal(e.g.founderContributedEquity,2_000_000);assert(loaded.modules.finance.validate(e.g).ok);
 assert(e.founderFund('shareholder-loan',3_000_000));assert.equal(e.g.founderShareholderLoan,3_000_000);assert.equal(e.g.companyDebt,3_000_000);assert(loaded.modules.finance.validate(e.g).ok);const p=e.g.personalCash;assert(e.repayFounderLoan(1_000_000));assert.equal(e.g.personalCash,p+1_000_000);assert.equal(e.g.founderShareholderLoan,2_000_000);assert(loaded.modules.finance.validate(e.g).ok);
}
// Office, not stores/profit/investment department, is company VC/M&A capability gate.
{
 const e=game(),startup=e.g.startups.find(x=>x.alive);e.g.companyCash=100_000_000;startup.fundingOpen=true;assert.equal(e.investStartup(startup.id,startup.minTicket,'company'),false);assert.equal(e.generateMATargets(true),false);const office=e.g.rentalOffices[0];assert.equal(e.contractOffice(office.id),true);startup.fundingOpen=true;assert.equal(e.investStartup(startup.id,startup.minTicket,'company'),true);assert.equal(e.generateMATargets(true),true);assert.equal(e.g.stores.length,0);
}
// UI is reachable through existing enhancer registry without a MutationObserver.
{
 const src=fs.readFileSync('js/play-runtime-compat.js','utf8');for(const token of ['founding-routes-complete-v2','data-fr-concept','data-fr-menu','data-fr-delegation','data-fr-limit-order','data-decision-cards'])assert(src.includes(token),token);assert(!src.includes('new MutationObserver'));
}
console.log('founding routes restaurant investment: ok');
'''
          write('tests/founding-routes-restaurant-investment-test.js',test)

          run=read('tests/run-all.js')
          old="  ['stock-trade-panel', 'tests/stock-trade-panel-test.js']\n]) {"
          new="  ['stock-trade-panel', 'tests/stock-trade-panel-test.js'],\n  ['founding-routes-restaurant-investment', 'tests/founding-routes-restaurant-investment-test.js']\n]) {"
          run=one(run,old,new,'run-all registration')
          write('tests/run-all.js',run)
          PY

          node tests/syntax-check.js
          node tests/static-check.js
          node tests/founding-routes-restaurant-investment-test.js
          node tests/transaction-regression-test.js
          node tests/save-compat-test.js
          node tests/finance-ledger-test.js
          node tests/market-engine-test.js
          node tests/market-integration-test.js
          node tests/supply-engine-test.js
          node tests/workforce-engine-test.js
          node tests/finance-startup-subsidiary-test.js
          node tests/venture-investment-outcome-test.js
          node tests/venture-investment-reachability-test.js
          node tests/ma-deal-room-test.js
          node tests/css-extraction-test.js
      - name: Verify net diff and self-remove
        shell: bash
        run: |
          rm -f .github/workflows/_stage-founding-routes-v2.yml
          python - <<'PY'
          import subprocess
          expected={
            'css/app.css','docs/LOAD_ORDER.md','js/app.js','js/engine.js','js/ma-deal-room.js','js/market.js','js/play-runtime-compat.js',
            'tests/finance-startup-subsidiary-test.js','tests/fixtures/extracted-css-baseline.css','tests/founding-routes-restaurant-investment-test.js','tests/run-all.js','tests/transaction-regression-test.js','tests/venture-investment-outcome-test.js','tests/venture-investment-reachability-test.js'
          }
          got=set(subprocess.check_output(['git','diff','--name-only','cf938896702b60398addd305eb20403972834bf5']).decode().split())
          if got!=expected: raise SystemExit(f'net diff mismatch expected={sorted(expected)} got={sorted(got)}')
          if any(x.startswith('.github/workflows/') for x in got): raise SystemExit('workflow leaked into net diff')
          css=open('css/app.css','rb').read(); baseline=open('tests/fixtures/extracted-css-baseline.css','rb').read()
          if css!=baseline: raise SystemExit('css baseline mismatch')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat: integrate ramen and investment founding routes'
          git push origin HEAD:feature/founding-routes-complete-v2

name: Phase 6B-2 Diagnostic
on:
  pull_request:
jobs:
  strategy-matrix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm run test:transaction > phase6b2-strategy.log 2>&1 || true
      - run: node tests/strategy-balance-calibration-test.js >> phase6b2-strategy.log 2>&1 || true
      - run: node tests/phase6b2-strategy-matrix-diagnostic.js >> phase6b2-strategy.log 2>&1 || true
      - uses: actions/upload-artifact@v4
        with:
          name: phase6b2-strategy
          path: phase6b2-strategy.log

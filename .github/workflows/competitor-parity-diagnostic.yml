name: Competitor Parity Diagnostic
on:
  pull_request:
jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm run test:javascript
      - run: npm run test:modules
      - name: Run save version 9 migration
        shell: bash
        run: |
          set +e
          npm run test:save-v9 > save-v9.log 2>&1
          code=$?
          cat save-v9.log
          exit $code
      - name: Run competitor parity compatibility
        shell: bash
        run: |
          set +e
          npm run test:competitor-parity > competitor-parity.log 2>&1
          code=$?
          cat competitor-parity.log
          exit $code
      - name: Upload diagnostic logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: competitor-parity-failure
          path: |
            save-v9.log
            competitor-parity.log
          if-no-files-found: warn

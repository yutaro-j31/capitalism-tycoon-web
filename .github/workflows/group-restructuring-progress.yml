name: Group Restructuring Progress

on:
  pull_request:
    paths:
      - 'js/group-restructuring-progress.js'
      - 'tests/group-restructuring-progress-test.js'
      - 'play.html'
      - 'tests/run-all.js'
      - '.github/workflows/group-restructuring-progress.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node --check js/group-restructuring-progress.js
      - run: node tests/group-restructuring-progress-test.js

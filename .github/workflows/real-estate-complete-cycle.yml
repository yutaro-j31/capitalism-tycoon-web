name: Real Estate Complete Cycle

on:
  pull_request:
    paths:
      - 'js/real-estate-complete-cycle.js'
      - 'js/real-estate-complete-cycle-ui.js'
      - 'js/real-estate-capex-actuals-ui.js'
      - 'tests/real-estate-complete-cycle-test.js'
      - '.github/workflows/real-estate-complete-cycle.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node tests/real-estate-complete-cycle-test.js

# PEモード 実装タスク一覧

**参照設計書: `docs/PE_MODE_DESIGN.md`（数値・根拠はすべてそこにある）**

このファイルは実装の順序と粒度を定める。
**1タスク = 1PR。複数タスクをまとめて実行しない。**

指示の例:
```
docs/PE_MODE_TASKS.md の T3 を実装して。
```

---

## 全タスクで必ず守ること

以下はこのリポジトリの絶対条件。**どのタスクでも例外なく守る。**

- `SAVE_KEY` は `capitalism_tycoon_web_v1` のまま変更しない
- `saveVersion` は `9` のまま。マイグレーションが必要な設計にしない
- **旧セーブ互換**を壊さない。新規stateはトップレベル追加か任意フィールドにし、
  未定義時は既存挙動と完全に一致させる
- **決定論を壊さない**。`Math.random()` を新規に呼ばない。
  `js/ma-deal-room.js` の `hash()` / `unit()` / `between()` と同じハッシュ方式を使う。
  既存のRNG消費順序を変えない
- 会計整合性を壊さない。現金・資産・損益の増減は既存の `finance.event()` 経由で記録する
- 会社資産と個人資産の分離を維持する
- 配列は上限を設けて `slice` する（既存の方針に合わせる）
- iPhone Safari で動作する構成を維持する
- 既存の release / canonical / WebKit ゲートを弱めない
- **表示だけ追加して計算と連動しない状態を作らない**
- 各タスクで `tests/` にテストを追加する

## 進め方

- T1〜T4 が終わった時点で、**既存のM&A画面で入札の面白さを試せる**。
  ここが最初の分岐点。手応えがなければ T5 以降に進む前に設計を見直す
- T5 以降は新規の器を作るため、確認できるUIが揃うまで時間がかかる
- UIは別途（ChatGPT / Codex 側）で実装するため、
  本タスク一覧では **状態設計と計算のみ** を扱う。
  ただし T14 の最小UIだけは動作確認のために必要

---

# フェーズA: 入札システム（既存 ma-deal-room の拡張）

このフェーズは既存の `js/ma-deal-room.js` を土台にする。
設計書 §8 の通り、隠された真の価値（`maTargetTruthByID`）、観測値のブレ、
DD scopes、推奨価格レンジ、売り手の事情は**すでに実装済み**。それを壊さず拡張する。

## T1. 競合PEロースター

**前提:** なし（最初のタスク）

**内容:**
- 新規モジュール `js/pe-rivals.js` を作る
- `state.peRivals` に5社の固定ロースターを持たせる。旧セーブでは空配列で無害
- 各社の性格は設計書 §5「競合PEの性格」の表に従う

| ファーム | 出現条件 | 攻撃性 |
|---|---|---|
| 外資系大手 | 大型案件のみ | 1.06 |
| 地場系 | 地元案件 | 1.02 |
| 再生特化 | 傷んだ会社 | 0.95 |
| 事業会社 | シナジーあり | 1.12 |
| 新興ファンド | 序盤のみ | 1.04 |

- 案件（`acquisitionTarget`）の属性から、どの社が参加するかを**決定論的に**判定する関数を提供する
- この段階では既存の入札処理を変更しない。判定関数とロースターのみ

**完了条件:**
- 同じ案件・同じ週なら常に同じ参加社が返る
- 旧セーブを読み込んでもエラーが出ない
- テスト: 参加判定の決定論性、5社すべてが何らかの案件で参加しうること

---

## T2. 競合入札の複数化

**前提:** T1

**内容:**
- `deal.competingBids` を配列として追加する
- **既存の `deal.competingBid`（単数）は消さない。** 旧セーブの進行中ディールが
  そのまま完了できるよう両対応にする
- `competitorResultScore` は**最高額の1社を相手にする形**で流用する。
  判定ロジックの書き換えは最小限に留める
- 参加社は T1 の判定関数から決める

**完了条件:**
- 旧セーブの `competingBid` を持つディールが、従来と完全に同じ結果になる
- 新規ディールでは複数社が入札し、最高額の1社と競う
- テスト: 旧セーブ互換、複数入札時の勝敗判定

---

## T3. ラウンド構造

**前提:** T2

**内容:**
- `deal.status` に2つ追加する
  - `indication`（初期意向表明）: 概要のみ開示。費用ゼロ。**参加者数が見える**
  - `final_bid`（最終入札）: 金額と条件を同時に提示
- 遷移: `available → screening → indication → diligence → ready → final_bid → offer_pending → accepted`
- `ACTIVE` と `DEAL_DEADLINE_STATUSES` の各Setに追加する
- 各ラウンドで「進む / 降りる」を選べる。降りるのはいつでも無料

**完了条件:**
- 既存の週次処理（`processDealWeek`）がそのまま回る
- 各ラウンドで降りられる
- テスト: 状態遷移、降りたディールが正しく終端化されること

---

## T4. 売り手選好タイプ

**前提:** T3

**内容:**
- 売り手に4タイプを持たせ、`sellerScore` に重みを足す

| 売り手 | 求めるもの | 効果 |
|---|---|---|
| 創業オーナーの引退 | 雇用維持・社名存続 | 約束すると後のリストラが封じられる |
| 大企業の切り離し | クロージング速度 | DDを省くと有利、だが誤差が残る |
| 破綻企業の債権者 | 現金一括 | レバレッジを使えない |
| 別PEからの二次売却 | 金額のみ | 純粋な価格勝負 |

- **最重要の実装方針:** 条件は「勝率を上げる装置」ではなく
  **「同じ勝率を安い値段で買う装置」**として作る。
  条件を飲むと売り手評価が上がる分、**札そのものを約11%下げられる**設計にする。
  条件を飲んで札も高いままにすると、勝ちやすくなるだけで勝者の呪いが増し逆効果になる
  （設計書 §9 失敗5）
- 飲んだ条件は `deal.acceptedTerms` に記録し、後で参照できるようにする

**完了条件:**
- 条件を飲むと、より低い札で落札できる
- 飲んだ条件が記録され、買収後に参照できる
- テスト: 各売り手タイプで条件の効果が出ること

---

## ★ 分岐点

**T4 完了時点で、既存のM&A画面で入札の面白さを試せる。**
ここで手応えを確認してから T5 以降に進む。

確認すること:
- 降りる判断に緊張感があるか
- 誰が入札に来るかを読む楽しさがあるか
- 条件で安く勝てたときに手応えがあるか

---

# フェーズB: ファンドという器

## T5. ファンド状態の基礎

**前提:** T4

**内容:**
- 新規モジュール `js/pe-fund.js`
- `state.peFirm` を追加する（トップレベル追加、旧セーブは `unlocked: false`）

```js
state.peFirm = {
  trackRecord: { score: 0, exits: [], realizedDPI: 0 },
  funds: [],
  ddSlotsPerYear: 3,
  unlocked: false
}
```

- ファンド1本の構造

```js
{ id, size, gpCommit, lps: [], terms: {fee, carry, hurdle},
  y0, cash, undrawn, distributed, deals: [], status }
```

- **ファンド期間10年、投資期間5年**（週次エンジンでは 520週 / 260週）
- **Exit代金は再投資できない。** 回収したら即分配する。
  これがFund IIを必要にする理由（設計書 §9 失敗3）
- この段階では計算のみ。組成UIはまだ作らない

**完了条件:**
- 旧セーブで `unlocked: false` になり、既存挙動に一切影響しない
- 週次処理でファンド期限が進む
- テスト: 旧セーブ互換、期限の進行、分配が再投資されないこと

---

## T6. トラックレコードとGP出資

**前提:** T5

**内容:**
- Exit時（会社売却・IPO・子会社売却）に4項目を記録する
  - 実現額（`personalCash` の増分）
  - 個人MOIC（Exit額 ÷ 創業時投下額）
  - 所要年数（`week` の差分）
  - 事業の質（黒字継続週数・従業員数）
- 合成してトラックレコードスコア（0〜100）。**初回Exitでは5〜15点**
- 要求GP出資比率: `20% − 18% × (score/100)^0.7`
- 報酬条件: 管理報酬 `1.5% + 1.0% × score/100`、
  成功報酬 `15% + 10% × score/100`、ハードル `10% − 2% × score/100`
- **GP出資は回収不能**。失敗すれば個人資産も消える
- PEモードの解禁条件: Exit経験1回以上

**完了条件:**
- 初回Exit後にPEモードが解禁される
- スコア5で組成可能額が個人資産10億に対して約28億になる
- テスト: スコア計算、GP出資比率の曲線、解禁条件

---

## T7. DPI / IRR と次号組成

**前提:** T6

**内容:**
- DPI（実現分配 ÷ 出資総額）と IRR を計算する
- 次号を組成できる条件: **DPI 1.2倍以上 かつ 資金消化80%以上**
- 未投資返却はDPIを下げない（1.0で戻る）が、**トラックレコードに加算されない**。
  これが「損はしていないのに前に進めない」焦りの源
- LP信頼度（次号の調達上限に掛かる係数）
  - DPI 1.2以上: ×1.12
  - DPI 1.0〜1.2: ×0.80
  - 元本割れ: ×0.55

**完了条件:**
- 実績が出ると次号が組成でき、規模が伸びる
- 資金消化80%未満だと実績が加算されない
- テスト: DPI計算、次号組成の条件判定、LP信頼度の増減

---

## T8. LP面談

**前提:** T7

**内容:**
- **LP面談は「決定」として作らない**（設計書 §9 失敗7）。
  検証の結果、LP構成をどう調整しても「集められるだけ集める」が常に最適だった。
  面談は**進捗の可視化・前号への評価・約束の管理**を担う
- 5種類のLP。会える条件は設計書 §3 の表に従う
- 付帯条件は**制約ではなく約束**として扱う。守れないと次号で不利になるだけ
- 既存LPは自動継続。前回の約束を守れたかで態度が変わる
- LPの要求は断れる（金額が減るだけでペナルティなし）

**完了条件:**
- 実績に応じて会えるLPが増える
- 約束の達成状況が記録され、次号の調達額に反映される
- テスト: LP解禁条件、約束の記録と反映

---

# フェーズC: 案件と制約

## T9. スロット制約とチーム

**前提:** T5

**内容:**
- **チーム上限は管理報酬から自動的に決まる**（1人あたり年2000万円）。
  制約を別途設ける必要はない
- **チーム上限は60人で固定**。超過分の管理報酬は素直に利益にする
  （現実の大型ファンドもフィーだけで儲かる。設計書 §9 失敗6）
- スロット数の上限は2つで決める
  - 1件あたりの下限投下額
  - LPの分散義務（1件はファンドの25%まで）
- 結果、スロットは **2 → 4 → 6 → 8** で頭打ち
- 注意分散: `attention = チーム人数 ÷ 案件数`。1件あたり1人を割ると改善が鈍る
- **保有期間: 1号は4年、2号以降は3年**が最適
  （1号は再投資できないので機会費用がゼロ）

**完了条件:**
- 28億のファンドで自分＋1人しか雇えず、スロットが2〜3件になる
- 1件あたり1人を割ると改善効果が落ちる
- テスト: チーム上限の算出、スロット上限、注意分散の効果

---

## T10. DD枠の有限化

**前提:** T9

**内容:**
- 現状のDDは金だけで制限されている。**枠を有限にする**
- `ddSlotsPerYear = 3 + パートナー数/4`
- 既存の `DILIGENCE_SCOPES`（confidence 0.40 / 0.72 / 0.94）はそのまま使う。
  検証で使った誤差18% / 10% / 5% とほぼ一致しているので再調整は不要

**完了条件:**
- 年間のDD回数が枠で制限される
- 枠を使い切ると精査できなくなる
- テスト: 枠の消費と年次リセット

---

## T11. 業種4帯

**前提:** T9

**内容:**
- 案件生成を4帯に分ける。詳細は設計書 §15

| 帯 | 規模 | 取得倍率 | レバ | 腕の乗り |
|---|---|---|---|---|
| 5本柱系 | 20〜1,500億 | 8.0 | 3.5 | **0.28** |
| 小型承継 | 3〜40億 | 5.5 | 4.5 | 0.04 |
| 中型 | 30〜400億 | 7.0 | 4.0 | 0.12 |
| 大型 | 300〜4,000億 | 8.5 | 3.5 | 0.10 |

- **リターンを削らず、規模帯で棲み分けさせる。**
  小型承継は大型ファンドでは消化できない（分散義務と最低投下額で自然に効く）
- 出口の可否
  - 5本柱系: 売却 / IPO / **自分で経営**
  - それ以外: 売却 / IPO（対応する株式市場セクターがある帯のみ）
- **案件数は年4件で固定。市況は数ではなく中身（質）を変える**
  - 好況: 優良案件が高値で出る。買い手も多い
  - 不況: 傷んだ案件が安く出る。買い手が減る
  - **年1件まで落とさないこと。腕が完全に消える**（設計書 §5）

**完了条件:**
- ファンド規模に応じて打てる帯が変わる
- 年4件が維持される
- テスト: 帯ごとの案件生成、規模による絞り込み、供給数の下限

---

## T12. 共同投資

**前提:** T7, T11

**内容:**
- LPが案件ごとに追加出資する仕組み
- 枠はファンド規模の**1倍**まで
- 共同投資分のキャリーは**フルの半分**（実績次第で 7.5〜12%）。
  **ゼロにすると機能が死ぬ**（規模の逓減で質が落ちるのに収入がなく、手間だけ増える）
- 管理報酬は共同投資分にはかからない
- 解禁は実績連動（数値上は不要だが体験として）
- 案件ごとに「共同投資を入れる（規模を取る）」「全額ファンドで打つ（報酬率を取る）」を選べる

**完了条件:**
- 共同投資で分散義務を超える大型案件が打てる
- 共同投資分の報酬が半分になる
- テスト: 枠の上限、報酬計算、分散義務との関係

---

# フェーズD: 人脈と経営

## T13. 人脈4経路

**前提:** T11

**内容:**
- 新規モジュール `js/pe-network.js`
- `state.peNetwork = { nodes: [], weeklyActions: 2, favorsOwed: [] }`。`nodes` は上限100
- **人脈は金で買えない。通貨は時間（週次アクション枠2回）**
- 4経路。詳細は設計書 §6.5

| 経路 | 通貨 | 上限寄与 |
|---|---|---|
| 紹介者（銀行・会計士） | 取引関係 | 22% |
| 長期の仕込み | 時間 | 55%（成功時） |
| 業界での評判 | 経営実績 | 32% |
| 既存投資先からの紹介 | 改善スコア | 30% |

- **1案件あたりの独占上限は40%**。構造的な縛り（業種・地域）だけでは48%まで伸びるため必須
- **紹介者には年5ポイントの減衰を入れる。** 入れないと最も安直な経路が全体の半分を占める
- 独占案件は「安く買える」ではなく**「独占交渉権」**。
  期間内にまとまらなければ公開入札に流れる
- ノードの供給源は5本柱の経営（仕入先、テナントオーナー、銀行支店長、雇ったCXO、Exit先の経営陣）
- **腕による格差は意図的に許容する。** 下手は独占0%、普通は投資先紹介0%で構わない

**完了条件:**
- 4経路それぞれが独立に育つ
- 独占比率が40%を超えない
- 紹介者が減衰する
- テスト: 各経路の判定、上限、減衰

---

## T14. 買収先を5本柱で経営する

**前提:** T11

**内容:**
- **このタスクが最も重い。** 既存の5本柱システムを
  「自社の事業」だけでなく「買収先の事業」としても動かす
- 5本柱系の案件を買収すると、既存の店舗経営UIが開く
- 価格・メニュー・出店を自分で決められる。**その経営結果が改善スコアになる**
- 経路3（業界での評判）と接続する。
  自分で経営して立て直した業種で、次の独占案件が来る
- 失敗すると逆に働く。従業員を切って売り抜けた実績は
  「あそこには売りたくない」という評判になる
- **動作確認のための最小UIはこのタスクに含める**（他タスクのUIは別途）

**完了条件:**
- 買収した5本柱系企業を既存の経営画面で操作できる
- 経営結果が改善スコアに反映される
- 会社資産と個人資産、自社事業と買収先事業の会計が混ざらない
- テスト: 買収先の経営、会計分離、改善スコアへの反映

---

# 検証タスク

## T15. 通し検証

**前提:** T14

**内容:**
- 固定seedで100年プレイテストを回す
- 設計書 §10 / §12 の数値と比較する

確認する数値:
- Fund I（28億）で半減（DPI 0.6未満）が発生しないこと
- 4年保有でFund II到達率が85%程度
- 天井（ファンド1本5兆円）で成長が線形になること
- 100年後の総資産が兆のオーダーに収まり、京にならないこと
- 腕による差が保たれていること（下手は天井未到達）
- 個人資産の利回りが年3%を超えないこと（5%超で再爆発する）

**完了条件:**
- 上記すべてを満たす
- NaN・Infinity が発生しない
- セーブサイズが上限内に収まる

---

# 参照

- 設計書: `docs/PE_MODE_DESIGN.md`
- 入札の土台: `js/ma-deal-room.js`
- 改善システムの土台: `js/pe-value-creation.js`
- 買収先の経営: `MASTER.businesses`（5本柱）
- 開発作法: `CLAUDE.md`, `docs/AUTONOMOUS_DEVELOPMENT_PLAYBOOK.md`
- UI規約: `DESIGN.md`, `docs/CSS_MAINTENANCE_GUIDE.md`

=== node --check js/map-phase2-canvas.js ===
EXIT=0

=== node tests/map-marker-building-affinity-test.js ===
PASS: every marker in every prefecture lands on a tile (no entity is dropped off the map by the new allow-list)
PASS: ACCEPTANCE 1: no tenant / office / store marker ever lands on logistics, industrial or warehouse stock
PASS: ACCEPTANCE 1 (cont.): no tenant / office / store marker ever lands on residential stock
PASS: ACCEPTANCE 2: tenant and store markers land on commercial stock first (>=80% preferred tier)
PASS: ACCEPTANCE 3: 住宅 property markers land on residential stock, every time
PASS: ACCEPTANCE 4: office markers land on office stock first (>=80% preferred tier)
PASS: office view-model preserves the real A/B/C rental-office grade without mutating state
FAIL: ACCEPTANCE 4b: C/B/A office candidates prefer small/mid/hero office stock respectively -- chiba: grade B office landed on commercial.hero
+ actual - expected

+ 'commercial.hero'
- 'office.mid'
PASS: unknown office grades retain the generic office affinity fallback
PASS: 物流 property markers land on logistics stock or industrial open ground, never on shops or housing
PASS: 土地 (land, not a building) is the only property kind placed on open ground -- and only on hardscape, never on parkland
PASS: no marker of any kind ever lands on civic buildings, the landmark, parkland, a billboard, a road or a footprint-reserved tile
PASS: the per-sprite override hook is live: commercial_billboard is a commercial.small asset yet hosts no marker anywhere
PASS: placement stays deterministic and order-independent under the new allow-list
PASS: a second, independently booted sandbox reproduces the exact same placement (no hidden cross-run state)
PASS: placement consumes no RNG: no Math.random and no simulation roll anywhere in the placement path
PASS: the city fabric itself is untouched: prototypes/map-world-preview.js still builds an identical district (markers never rewrite scenery)
PASS: scarcity degrades through the documented ladder for office: 400 markers in gunma exhaust the preferred tier and still never reach forbidden stock
PASS: scarcity degrades through the documented ladder for tenant: 400 markers in gunma exhaust the preferred tier and still never reach forbidden stock
PASS: scarcity degrades through the documented ladder for store: 400 markers in gunma exhaust the preferred tier and still never reach forbidden stock
PASS: the allow-list TABLE itself never offers industrial or residential stock to a leaseable kind (guards the table, not just the outcome)
PASS: the property allow-list TABLE keeps each property kind on stock that matches its own label
PASS: the visible pin is drawn by ::before and is strictly smaller than the button that carries the tap target
PASS: the tap target stays >=44px on both axes and is genuinely unclipped (clip-path clips hit testing too)
PASS: the visible pin shrank against the size this pass inherited (48x60)
PASS: the pin is centred on the button centre, so shrinking it did not move the anchor
PASS: the decluttering collision box tracks the visible pin, not the button, and one ring can clear a head-on collision within the cap
PASS: NEGATIVE: reverting to zone-based selection puts leaseable markers back on housing and empty ground
PASS: NEGATIVE: dropping SURFACE_GREEN out of the forbidden set (adding it to a kind) would let markers sit on parkland
PASS: NEGATIVE: removing the open-space guard from surfaceOfCell would make every empty plot a marker candidate again
PASS: NEGATIVE: putting the clip-path back on the button would collapse the pin and the tap target into one box again

30 passed, 1 failed
EXIT=1
